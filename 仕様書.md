# osu! Hub 仕様書

## 1. 目的

osu!に関するプレイ記録、アカウント同期、AIコーチング、練習管理、設定保存、Windows補助ツールを1か所にまとめる個人向けハブを作る。

## 2. Project Guide / Profile

採用Guide Version: `1.2.0`

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

正本は `project-meta.json`。

Source of Truth:

- Web Version: `data/site.json#siteVersion`
- Web Storage Schema: `js/storage.js#SCHEMA_VERSION`
- Desktop Version: `package.json#version`
- Desktop Update Metadata: `version.json`

正式RuntimeはVersion Metadataと分離し、`js/app.js`、`js/storage.js`、`js/osu-sync.js`等の安定Pathを使う。Version別Runtime Folderを正式構造として増やさない。

## 3. 想定利用者

- 主用途は個人利用
- 将来的に他のosu!プレイヤーでも使える構成にする
- 企業向けの複雑な権限管理より、操作性・分かりやすさ・安定性・軽量性を優先する

## 4. システム構成

```text
GitHub Pages (osu! Hub)
        ↓ HTTPS
Cloudflare Worker
        ↓ OAuth Client Credentials
osu!api v2

Browser IndexedDB
        ↑
Results / Coaching / Practice / Settings
```

Windows固有機能は別系統でElectron製 `osu Setup Launcher` が担当する。

## 5. Web機能

### Home

- Results / Coaching / Practice件数
- 平均Accuracy
- 最近のリザルト
- Account Syncへの主要導線
- Worker URL未設定時は `SETUP REQUIRED` を表示
- BrowserにWorker URL保存済みなら `READY` 表示
- Web VersionはHTMLへ直書きせず `js/site-meta.js` が `data/site.json` から表示

### Account Sync

入力:

- Cloudflare Worker URL
- osu! User ID またはユーザー名
- Ruleset
- 取得件数 1〜100
- Failスコアを含める / 含めない

動作:

1. `/health`でWorker設定を確認
2. `/api/sync`へ同期対象を送る
3. Workerがosu! OAuth Client Credentialsでアクセストークンを取得
4. osu! Userを解決
5. Recent Scoresを取得
6. Workerでosu! Hub用データへ正規化
7. Browser側でWorker Responseを再Validation
8. Browser側で `id = osu:<score id>` を再生成
9. IndexedDB `results`へ保存
10. 同一Score IDは更新し、重複追加しない
11. 既存の手入力Resultsは削除しない
12. API同期済みResultsの手動メモは再同期で維持する

通信:

- Production Worker URLはHTTPS必須
- `localhost` / `127.0.0.1`のみHTTPを許可
- Browser request timeout: 15秒（`data/site.json`で管理）
- Worker → osu! API timeout: 12秒
- timeout / API failure時はErrorを表示し、既存Localデータを削除しない

同期対象フィールド:

- score id
- beatmap / beatmapset id
- map name
- artist / title / difficulty / mapper
- play date / timestamp
- Accuracy
- Miss
- Combo
- PP
- Star Rating
- BPM
- AR / OD / CS / HP
- MOD
- Rank
- Pass / Fail
- Replay有無
- total score

### AI Coaching

- 複数リザルト画像のドラッグ&ドロップ
- セッション名、日付、練習目的、本人メモ
- ChatGPT提出用ZIP生成
- ChatGPT返却JSONの取込
- 分析履歴保存
- 画像は1枚20MBまで、最大100枚
- JSZip CDN失敗時はmanifest / prompt個別出力へFallback

AI返却Schema v1:

```json
{
  "schemaVersion": 1,
  "summary": "string",
  "strengths": ["string"],
  "weaknesses": ["string"],
  "patterns": ["string"],
  "recommendedPractice": [
    {"title": "string", "minutes": 15, "reason": "string"}
  ],
  "nextGoals": ["string"],
  "confidenceNotes": ["string"]
}
```

取込時にSchema Version、配列型、練習提案形式をValidationする。AI出力を無検証で事実扱いしない。

### Results

- API同期Resultsと手入力Resultsを同じObject Storeで扱う
- 手入力行は `source: manual`
- API同期行は `source: osu-api`
- 譜面名 / 日付 / MOD / Accuracy / Miss / Combo / PP / ★ / BPM / メモ
- 一覧表示 / 削除
- 削除前確認

### Stats

実装済み:

- 登録リザルト数
- 平均Accuracy
- 平均Miss
- 最高PP
- 直近Accuracy
- MOD別件数 / 平均Accuracy

未実装として明示:

- BPM別ACC
- ★別Miss
- Session比較
- Aim / Stream分類
- PP推移
- AI Coach履歴比較

### Practice

- 日付
- カテゴリ
- 練習内容
- 時間
- メモ
- 完了チェック
- 削除前確認

### Settings

- プレイヤー名
- モード
- DPI / osu!感度
- Tablet Area / Offset
- メモ
- WebデータJSON Backup / Import
- Account Sync設定はIndexedDB `settings`へ保存

## 6. データ / 保存

IndexedDB:

```text
DB: osuHubDB
DB Version: 1
Schema Version: 1
stores:
  results
  coaching
  practice
  settings
```

### Backup / Import / Recovery

現在のUI Importは完全置換RestoreではなくMerge方式。同じKeyは更新されるため、既存データ変更を伴うImportとしてRecovery対象にする。

処理順:

```text
1. JSON parse
2. Top-level Schema / Version確認
3. 全Store / Record / ID / 数値範囲をValidation
4. 想定外Store / 巨大Recordを拒否
5. 現在DataをRecovery Snapshotとして取得
6. Import Dataをnormalize
7. 複数Storeを1 IndexedDB transactionでmerge
8. Import対象Recordを読み戻してValidation
9. 成功なら完了
10. Verification失敗ならRecovery Snapshotから全StoreをRollback
```

Transaction自体が失敗した場合はIndexedDB atomicityにより途中状態をcommitしない。

Validation対象:

- Schema / Version
- Store名
- Array / Object型
- Record ID / Key
- Store内重複ID
- Result Accuracy / Miss / Combo / PP / ★ / BPM範囲
- Practice minutes範囲
- osu-api Resultの `osuScoreId` と `id=osu:<score id>` 整合性
- 1 RecordあたりのJSONサイズ上限

完全置換Restoreを将来追加する場合も、同じ `validate → recovery → replace → read-back verify → rollback` 順序を必須にする。

## 7. Architecture / DOM責務

- 自前DOMは原則としてそのRenderer自身が最終形を生成する
- Stable Web RuntimeでMutationObserverによる恒久的DOM Patchを使わない
- UI → Storage / External APIの責務を分離する
- Versioned Patch JSを恒久構造にしない
- Version MetadataはRuntime Pathと分離する

## 8. osu! API / Cloudflare Worker

Worker配置:

```text
cloudflare/worker/
```

Secret:

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

禁止:

- GitHubへClient Secretを保存しない
- GitHub PagesのHTML/JSへClient Secretを埋め込まない
- `.dev.vars` / `.env`をコミットしない

Worker公開API:

```text
GET /health
GET /api/sync?user=...&mode=osu&limit=100&include_fails=1
```

Workerは任意のosu! API URLを中継する汎用Proxyにしない。

CORSは `ALLOWED_ORIGINS` で許可Originを制限する。

## 9. UI / Accessibility

- `nav`へaria-label
- Form labelは対象Inputと `for` / `id` で関連付け
- 主要操作はbutton / linkを利用し、クリック専用divを使わない
- AI Coaching Dropzoneはbuttonとして操作可能
- `:focus-visible`を表示
- `prefers-reduced-motion`を尊重
- 900px以下でもNavigationを非表示にせず横Scrollで利用可能
- ページ全体の不要な横overflowを避ける
- Loading / Empty / Error / Success状態を必要箇所で表示

## 10. Desktop Launcher

従来のElectron製 `osu Setup Launcher v0.17.0` は削除しない。

崩してはいけない仕様:

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer の一括起動
- osu!本体の自動操作・プレイ補助は行わない
- ユーザー設定を更新時に維持する
- 外部ツール本体、秘密情報、個人PCパスを公開リポジトリへ直接含めない
- 起動失敗時はログに理由を残す

Windows固有機能は実Windowsで未確認なら未確認として扱う。

## 11. GitHub Pages

- リポジトリ直下 `index.html` を入口にする
- 相対パスを使い `/osu-hub/` 配下で動作する
- Cloudflare WorkerコードやElectronコードをPages Artifactへ含めない
- Web Versionは `data/site.json` を正本にする

公開URL:

```text
https://elitemay.github.io/osu-hub/
```

## 12. 自動Validation

`tests/validate-web.mjs` と `.github/workflows/check-web.yml` で以下を検査する。

- JS / MJS構文
- JSON構文
- HTMLローカル参照
- ID重複
- `label`関連付け
- Navigation aria-label
- HTMLへのWeb Version直書き
- Guide Version / Project Profile
- Desktop Version整合
- Production Worker HTTPS
- timeout設定範囲
- mobile Navigation非表示の再混入
- `focus-visible`
- reduced-motion
- 公開WebへのSecret値混入
- `.env` / `.dev.vars`誤追跡
- Version付きRuntime Path再混入
- Stable RuntimeでMutationObserver再混入
- Import Recovery / Read-back Verification / Rollback Guard

Pull Requestとmain pushの両方で実行する。

## 13. 崩してはいけない仕様

- Client Secretを公開しない
- 個人プレイデータをGitHubへ自動保存しない
- 手入力ResultsをAPI同期で消さない
- 同一osu! Score IDの重複を増やさない
- IndexedDB保存とJSON Backup / Importを維持する
- APIが使えなくても手入力Results等のローカル機能は使える
- Desktop LauncherをWeb化のために削除しない
- Stable RuntimeをVersion別Folderへ増殖させない
- 自前DOMをMutationObserver Patchで完成させない
- 実機未確認を確認済みと扱わない

## 14. 完成条件

GitHub側:

- Current Guide / Project Profileを記録
- Account Sync / Local機能が実装されている
- Secretが公開Artifactへ含まれない
- Save / Import / API Responseの主要Validationがある
- README / 仕様書 / 作業報告が現行構成と一致する
- Pull Request CIが成功する
- merge後の最終mainでCIが成功する
- GitHub Pages deployが成功する

本番接続側:

- Cloudflare Workerをdeploy
- osu! OAuth SecretをCloudflareへ設定
- 実アカウントでRecent Scoresを取得
- Results / Statsへ反映されることを実ブラウザで確認

本番接続側が未確認の場合、Cloudflare同期を含むプロジェクト全体を完成扱いにしない。

## 15. 今後の候補

- Recent Scoresの差分・ページング同期
- Best Scores同期
- 同期済みResultsをAI Coachingへ直接追加
- BPM / ★ / AR / OD別統計
- `.osu` 譜面解析
- `.osr` Replay管理・解析
- Beatmap Collections
- Skin管理
- Aim / Stream / Burst / Speed / Readingタグ
- AI分析からPracticeへのワンクリック反映
- Session比較
- 目標管理
