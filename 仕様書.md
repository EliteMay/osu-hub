# osu! Hub 仕様書

## 1. 目的

osu!に関するプレイ記録、アカウント同期、AIコーチング、練習管理、設定保存、Windows補助ツールを1か所にまとめる個人向けハブを作る。

## 2. 想定利用者

- 主用途は個人利用
- 将来的に他のosu!プレイヤーでも使える構成にする
- 企業向けの複雑な権限管理より、軽量・分かりやすさ・操作性を優先する

## 3. システム構成

```text
GitHub Pages (osu! Hub)
        ↓ HTTPS
Cloudflare Worker
        ↓ OAuth Client Credentials
osu!api v2
```

Windows固有機能は別系統でElectron製 `osu Setup Launcher` が担当する。

## 4. Web機能 v0.2.0

### Home
- Results / Coaching / Practice件数
- 平均Accuracy
- 最近のリザルト
- Account Syncへの主要導線

### Account Sync

入力:
- Cloudflare Worker URL
- osu! User ID またはユーザー名
- Ruleset
- 取得件数 1〜100
- Failスコアを含める / 含めない

動作:
1. `/health`でWorker設定を確認できる
2. `/api/sync`へ同期対象を送る
3. Workerがosu! OAuth Client Credentialsでアクセストークンを取得
4. osu! Userを解決
5. Recent Scoresを取得
6. Workerでosu! Hub用データへ正規化
7. ブラウザのIndexedDB `results`へ保存
8. 同一Score IDは`osu:<score id>`で更新し、重複追加しない
9. 既存の手入力Resultsは削除しない
10. API同期済みResultsの手動メモは再同期で維持する

同期対象フィールド:
- score id
- beatmap / beatmapset id
- map name
- artist / title / difficulty / mapper
- play date / timestamp
- Accuracy
- Miss
- Combo
- PP
- Star Rating
- BPM
- AR / OD / CS / HP
- MOD
- Rank
- Pass / Fail
- Replay有無
- total score

### AI Coaching
- 複数リザルト画像のドラッグ&ドロップ
- セッション名、日付、練習目的、本人メモ
- ChatGPT提出用ZIP生成
- ChatGPT返却JSONの取込
- 分析履歴保存
- v0.2.0時点では画像中心。同期済みResultsの直接添付は次段階

### Results
- API同期Resultsと手入力Resultsを同じObject Storeで扱う
- 譜面名 / 日付 / MOD / Accuracy / Miss / Combo / PP / ★ / BPM / メモ
- 一覧表示 / 削除

### Stats
- 登録リザルト数
- 平均Accuracy
- 平均Miss
- 最高PP
- 直近Accuracy
- MOD別件数 / 平均Accuracy

### Practice
- 日付
- カテゴリ
- 練習内容
- 時間
- メモ
- 完了チェック

### Settings
- プレイヤー名
- モード
- DPI / osu!感度
- タブレットArea / Offset
- メモ
- WebデータのJSONバックアップ / 復元
- Account Sync設定もIndexedDB `settings`へ保存

### Desktop Tools
- osu Setup Launcher説明
- GitHub Releasesへのダウンロード導線
- WebとDesktop機能を分離

## 5. データ構成

IndexedDB:

```text
DB: osuHubDB
stores:
  results
  coaching
  practice
  settings
```

`results`は手入力とAPI同期を共存させる。

API同期データ例:

```json
{
  "id": "osu:123456789",
  "source": "osu-api",
  "osuScoreId": "123456789",
  "mapName": "Artist - Title [Difficulty]",
  "accuracy": 98.42,
  "miss": 1,
  "combo": 850,
  "pp": 205.3,
  "stars": 6.2,
  "bpm": 210,
  "mods": "HDHR",
  "passed": true
}
```

## 6. osu! API / Cloudflare Worker

Worker配置:

```text
cloudflare/worker/
```

Secret:

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

禁止:
- GitHubへClient Secretを保存しない
- GitHub PagesのHTML/JSへClient Secretを埋め込まない
- `.dev.vars` / `.env`をコミットしない

Worker公開API:

```text
GET /health
GET /api/sync?user=...&mode=osu&limit=100&include_fails=1
```

公開APIを無制限プロキシにせず、osu! Hubで必要なエンドポイントだけを実装する。

## 7. AI連携

ChatGPT PlusとAPI利用は別扱いのため、有料APIキーを必須にしない。

基本フロー:
1. Webで提出データ作成
2. ユーザーがChatGPTへアップロード
3. ChatGPTが指定JSON形式で分析
4. JSONをWebへ戻す
5. IndexedDBに履歴保存

将来はAccount Syncで取得したプレイデータを提出ZIPへ直接含める。

## 8. Desktop Launcher

従来のElectron製 `osu Setup Launcher v0.17.0` は削除しない。

崩してはいけない仕様:
- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer の一括起動
- osu!本体の自動操作・プレイ補助は行わない
- ユーザー設定を更新時に維持する
- 外部ツール本体、秘密情報、個人PCパスを公開リポジトリへ直接含めない
- 起動失敗時はログに理由を残す

## 9. GitHub Pages

- リポジトリ直下`index.html`を入口にする
- 相対パスを使い`/osu-hub/`配下で動作する
- Cloudflare WorkerコードやElectronコードをPages公開Artifactへ含めない

公開URL:

```text
https://elitemay.github.io/osu-hub/
```

## 10. 崩してはいけない仕様

- Client Secretを公開しない
- 個人プレイデータをGitHubへ自動保存しない
- 手入力ResultsをAPI同期で消さない
- 同一osu! Score IDの重複を増やさない
- IndexedDB保存とJSONバックアップを維持する
- APIが使えなくても手入力Results等のローカル機能は使える
- Desktop LauncherをWeb化のために削除しない

## 11. 完成条件 v0.2.0

GitHub側:
- Account Sync画面が存在する
- Cloudflare Workerソースが存在する
- Secretがリポジトリへ含まれていない
- Recent ScoresをResults形式へ変換できる実装がある
- 重複更新ロジックがある
- README / 仕様書 / 作業報告書が現行構成と一致する
- JavaScript構文・JSON・ローカルリンクのCIが成功する

本番接続側:
- Cloudflare Workerをdeploy
- osu! OAuth SecretをCloudflareへ設定
- 実アカウントでRecent Scoresを取得
- Results / Statsへ反映されることを実ブラウザで確認

本番接続側が未確認の場合はプロジェクト全体を完成扱いにしない。

## 12. 今後の候補

- Recent Scoresの差分・ページング同期
- Best Scores同期
- 同期済みResultsをAI Coachingへ直接追加
- BPM / ★ / AR / OD別統計
- `.osu` 譜面解析
- `.osr` Replay管理・解析
- Beatmap Collections
- Skin管理
- Aim / Stream / Burst / Speed / Readingタグ
- AI分析からPracticeへのワンクリック反映
- Session比較
- 目標管理
