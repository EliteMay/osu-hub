# osu! Hub 仕様書

## 1. 目的

osu!のプレイ記録、アカウント同期、AIコーチング、練習管理、設定保存、Windows補助ツールを1か所にまとめる個人向けハブ。

## 2. Project Guide / Profile

採用Guide Version: `1.2.0`

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

Source of Truth:

- Web Version: `data/site.json#siteVersion`
- Web Storage Schema: `js/storage.js#SCHEMA_VERSION`
- Desktop Version: `package.json#version`
- Desktop Update Metadata: `version.json`

## 3. システム構成

```text
GitHub Pages
   ↓ HTTPS
Cloudflare Worker
   ↓ OAuth Client Credentials
osu!api v2

Browser IndexedDB
   ↑
Results / Coaching / Practice / Settings
```

Windows固有機能はElectron製 `osu Setup Launcher` が担当する。

## 4. Account Sync

入力:

- Cloudflare Worker URL
- osu! User ID / username
- Ruleset
- 取得件数 1〜100
- Failを含める / 含めない

処理:

1. `/health`でWorker確認
2. `/api/sync`へ同期条件を送信
3. WorkerがClient Credentials Tokenを取得または再利用
4. osu! Userを解決
5. Recent Scores取得
6. Workerで正規化
7. Browser側でResponse再Validation
8. `id = osu:<score id>` を再生成
9. IndexedDB `results`へ保存
10. 同一Score IDは更新、手入力Resultsは維持

同期フィールド:

- Score / Beatmap / Beatmapset ID
- Map / Artist / Title / Difficulty / Mapper
- Date / Timestamp
- Accuracy / Miss / Combo / PP
- Star / BPM / AR / OD / CS / HP
- MOD / Rank / Pass / Fail / Replay / Total Score

## 5. 外部API Reliability / Rate Limit

- Browser timeout: 15秒
- Worker → osu! timeout: 12秒
- `response.ok`を必ず確認
- timeout / API失敗時もLocalデータを削除しない
- OAuth 429 / API 429を明示的に扱う
- `Retry-After`がある場合はWorker Response / Headerへ引き継ぐ

### OAuth Token Cache

Client Credentials Tokenは有効期限内に再利用する。

```text
Memory cache
   ↓ miss
Cloudflare Cache API
   ↓ miss
osu! OAuth /oauth/token
```

- 同一Isolate内の同時Token取得は `tokenRefreshPromise` で1回へ集約
- 401時のみToken Cacheを破棄し、1回だけ再取得
- Client Secret / Access Tokenは公開Responseへ返さない

### Sync Cache

同じ `user / mode / limit / include_fails` 条件は60秒Cache APIで再利用する。

目的:

- 同一ユーザーへの1分未満の再pollingを避ける
- 連打によるAPI Request増加を防ぐ
- osu! APIをデータベース代わりに毎回再取得しない

## 6. Cloudflare Worker

公開API:

```text
GET /health
GET /api/sync?user=...&mode=osu&limit=100&include_fails=1
```

Secret:

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

GitHub Actions Secret:

```text
CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_TOKEN
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

禁止:

- SecretをGitHub通常ファイル / Pagesへ保存しない
- `.dev.vars` / `.env`をcommitしない
- 任意URLを中継する汎用Proxyにしない

CORSは `ALLOWED_ORIGINS` で制限する。

## 7. Deployment

`Deploy osu Hub API Worker` Workflowを使用する。

- 手動 `workflow_dispatch`
- mainの `cloudflare/worker/**` 変更時は自動deploy
- deploy workflow自身の変更時も自動deploy
- deploy後に `/health` を検証

## 8. Results / Stats / Practice / Settings

- Results: API同期 + 手入力
- Stats: 平均ACC / 平均Miss / 最高PP / 直近ACC / MOD別
- Practice: 練習内容 / 時間 / 完了管理
- Settings: DPI / 感度 / Tablet / JSON Backup / Import

API停止時もこれらLocal機能を利用可能にする。

## 9. AI Coaching

- 複数画像
- セッション情報
- ChatGPT提出ZIP
- Schema v1 JSON取込
- AI結果はValidation後に保存
- CDN失敗時はJSON/TXT個別出力Fallback

## 10. Data / Backup

```text
DB: osuHubDB
DB Version: 1
Schema Version: 1
stores:
  results
  coaching
  practice
  settings
```

Import:

```text
parse
→ 全体Validation
→ Recovery Snapshot
→ transaction write
→ read-back verification
→ failure時rollback
```

今回のOAuth 429修正ではIndexedDB Schema / Store / Backup Schemaを変更しない。

## 11. UI / Accessibility

- Navigation aria-label
- label / input関連付け
- focus-visible
- reduced-motion
- MobileでもNavigationを維持
- Loading / Empty / Error / Success表示

## 12. Desktop Launcher

`osu Setup Launcher v0.17.0` を維持する。

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer
- osu!本体の自動操作はしない
- userData設定を維持
- 外部exe / Secret / 個人Pathを公開Repoへ含めない

## 13. Validation

`tests/validate-web.mjs` / `Check web` で以下を検査する。

- JS / JSON / HTML
- Version / Project Profile
- Secret混入
- Worker HTTPS / timeout
- OAuth Token共有Cache
- 同期60秒Cache
- 429 handling / Retry-After
- Worker自動deploy path filter
- Import Recovery / Rollback
- Accessibility

PRとmain pushの両方で実行する。

## 14. 完成条件

- PR CI成功
- merge後main CI成功
- GitHub Pages deploy成功
- Worker自動deploy成功
- `/health`成功
- 実アカウントRecent Scores同期成功
- Results / Statsへ反映

実アカウント同期が未確認ならAccount Sync全体を完成扱いにしない。

## 15. 今後

- Recent Scoresの差分 / Pageing
- Best Scores同期
- 同期済みResults → AI Coaching
- BPM / ★ / AR / OD別統計
- `.osu` / `.osr`解析
- Session比較 / Goal管理
