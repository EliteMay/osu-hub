# osu! Hub 仕様書

## 1. 目的

osu!のプレイ記録、アカウント同期、AIコーチング、練習管理、設定保存、Windows補助ツールを1か所にまとめる個人向けハブ。

## 2. Project Guide / Profile

採用Guide Version: `1.2.0`

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

Source of Truth:

- Web Version: `data/site.json#siteVersion`
- Web Storage Schema: `js/storage.js#SCHEMA_VERSION`
- Desktop Version: `package.json#version`
- Desktop Update Metadata: `version.json`

## 3. システム構成

```text
GitHub Pages
   ↓ HTTPS
Cloudflare Worker
   ↓ public profile / public Recent Scores
osu! Web

Browser IndexedDB
   ↑
Results / Coaching / Practice / Settings
```

Account Syncはosu! OAuth Client Credentialsを使用しない。Windows固有機能はElectron製 `osu Setup Launcher` が担当する。

## 4. Account Sync

入力:

- Cloudflare Worker URL
- osu! User ID / username
- Ruleset
- 取得件数 1〜100
- Failを含める / 含めない

処理:

1. `/health`でWorker確認
2. `upstreamMode=public-web` / `oauthRequired=false` をBrowser側で確認
3. `/api/sync`へ同期条件を送信
4. Workerが公開プロフィールページを取得
5. `data-react="profile-page"` の `data-initial-data` から公開User / Statisticsを抽出
6. 解決したUser IDで公開Recent Scores経路を取得
7. Workerでosu! Hub用JSONへ正規化
8. Browser側でResponse再Validation
9. `id = osu:<score id>` を再生成
10. IndexedDB `results`へ保存
11. 同一Score IDは更新、手入力Resultsは維持

同期フィールド:

- Score / Beatmap / Beatmapset ID
- Map / Artist / Title / Difficulty / Mapper
- Date / Timestamp
- Accuracy / Miss / Combo / PP
- Star / BPM / AR / OD / CS / HP
- MOD / Rank / Pass / Fail / Replay / Total Score
- User ID / username / avatar / country / PP / rank / profile accuracy等の公開Statistics

## 5. External Reliability / Rate Limit

- Browser timeout: 15秒
- Worker → osu! timeout: 12秒
- `response.ok`を必ず確認
- timeout / upstream失敗時もLocalデータを削除しない
- 429を明示的に扱う
- `Retry-After`がある場合はWorker Response / Headerへ引き継ぐ

### Sync Cache

同じ `user / mode / limit / include_fails` 条件は60秒Cache APIで再利用する。

目的:

- 同一ユーザーへの短時間の再pollingを避ける
- 連打による上流Request増加を防ぐ
- osu!公開Webをデータベース代わりに毎回再取得しない

### OAuth非依存

現行Account Syncは以下を使用しない。

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
/oauth/token
```

理由はCloudflare WorkerからOAuth Token Endpointを利用した旧方式で429が継続したため。公開情報のみを扱う現在の要件では、osu!公式Webが利用する公開経路で必要データを取得する。

## 6. Public Web Dependency

Account Syncはosu! Webの以下の公開実装へ依存する。

- 公開User Profile URL
- Profile HTMLの `data-react="profile-page"`
- `data-initial-data`
- 公開 `/users/{id}/scores/recent` 相当のWeb Route

これはosu!api v2のOAuth Endpointとは異なるため、osu!側のWeb実装変更で動かなくなる可能性がある。

MUST:

- HTML / JSONをValidationしてから利用する
- 想定形式でなければ保存せずErrorにする
- 任意URLを中継する汎用Proxyへ拡張しない
- 取得対象を公開プロフィール・公開スコアに限定する

## 7. Cloudflare Worker

公開API:

```text
GET /health
GET /api/sync?user=...&mode=osu&limit=100&include_fails=1
```

`/health`:

```json
{
  "ok": true,
  "service": "osu-hub-api",
  "apiVersion": 1,
  "configured": true,
  "upstreamMode": "public-web",
  "oauthRequired": false
}
```

GitHub Actions Secret:

```text
CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_TOKEN
```

禁止:

- SecretをGitHub通常ファイル / Pagesへ保存しない
- `.dev.vars` / `.env`へ不要なosu! Secretを要求しない
- 任意URLを中継する汎用Proxyにしない

CORSは `ALLOWED_ORIGINS` で制限する。

## 8. Deployment

`Deploy osu Hub API Worker` Workflowを使用する。

- 手動 `workflow_dispatch`
- mainの `cloudflare/worker/**` 変更時は自動deploy
- deploy workflow自身の変更時も自動deploy
- deploy後 `/health` を検証
- `upstreamMode=public-web` と `oauthRequired=false` を検証

## 9. Results / Stats / Practice / Settings

- Results: osu!同期 + 手入力
- Stats: 平均ACC / 平均Miss / 最高PP / 直近ACC / MOD別
- Practice: 練習内容 / 時間 / 完了管理
- Settings: DPI / 感度 / Tablet / JSON Backup / Import

外部同期停止時もLocal機能を利用可能にする。

## 10. AI Coaching

- 複数画像
- セッション情報
- ChatGPT提出ZIP
- Schema v1 JSON取込
- AI結果はValidation後に保存
- CDN失敗時はJSON/TXT個別出力Fallback

## 11. Data / Backup

```text
DB: osuHubDB
DB Version: 1
Schema Version: 1
stores:
  results
  coaching
  practice
  settings
```

Import:

```text
parse
→ 全体Validation
→ Recovery Snapshot
→ transaction write
→ read-back verification
→ failure時rollback
```

Web 0.2.4ではIndexedDB Schema / Store / Backup Schemaを変更しない。Migration不要。

## 12. UI / Accessibility

- Navigation aria-label
- label / input関連付け
- focus-visible
- reduced-motion
- MobileでもNavigationを維持
- Loading / Empty / Error / Success表示
- Account Sync画面でosu! Secret不要を明示

## 13. Desktop Launcher

`osu Setup Launcher v0.17.0` を維持する。

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer
- osu!本体の自動操作はしない
- userData設定を維持
- 外部exe / Secret / 個人Pathを公開Repoへ含めない

## 14. Validation

`tests/validate-web.mjs` / `Check web` で以下を検査する。

- JS / JSON / HTML
- Version / Project Profile
- Secret混入
- Worker HTTPS / timeout
- `HTMLRewriter` / Profile marker / Recent Scores path
- WorkerからOAuth Secret・`/oauth/token`依存が再混入していないこと
- 同期60秒Cache
- 429 handling / Retry-After
- Worker自動deploy path filter
- Import Recovery / Rollback
- Accessibility

PRとmain pushの両方で実行する。

## 15. 完成条件

- PR CI成功
- merge後main CI成功
- GitHub Pages deploy成功
- Worker自動deploy成功
- `/health`成功かつ `public-web / oauthRequired=false`
- 実アカウントRecent Scores同期成功
- Results / Statsへ反映

実アカウント同期が未確認ならAccount Sync全体を完成扱いにしない。

## 16. 今後

- Public Web Route変更検知の改善
- Recent Scoresの差分 / Paging
- Best Scores同期
- 同期済みResults → AI Coaching
- BPM / ★ / AR / OD別統計
- `.osu` / `.osr`解析
- Session比較 / Goal管理
