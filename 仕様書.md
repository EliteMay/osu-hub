# osu! Hub 仕様書

## 1. 目的

osu!のプレイ記録、アカウント同期、AIコーチング、練習管理、設定保存、Windows補助ツールを1か所にまとめる個人向けハブ。

## 2. Project Guide / Profile

採用Guide Version: `1.2.0`

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

Source of Truth:

- Web Version: `data/site.json#siteVersion`
- Web Storage Schema: `js/storage.js#SCHEMA_VERSION`
- Desktop Version: `package.json#version`
- Desktop Update Metadata: `version.json`

## 3. システム構成

```text
GitHub Pages
   ↓ HTTPS
Cloudflare Worker
   ↓ Bearer: OSU_ACCESS_TOKEN
osu! API v2

GitHub Actions
   ↓ Client Credentials
osu! /oauth/token
   ↓ short-lived Access Token
Cloudflare Worker Secret

Browser IndexedDB
   ↑
Results / Coaching / Practice / Settings
```

Windows固有機能はElectron製 `osu Setup Launcher` が担当する。

## 4. Account Sync

入力:

- Cloudflare Worker URL
- osu! User ID / username
- Ruleset
- 取得件数 1〜100
- Failを含める / 含めない

ブラウザへosu! Client ID / Client Secretを入力させない。

処理:

1. `/health`でWorker確認
2. `configured=true` を確認
3. `upstreamMode=api-v2-preissued-token` を確認
4. `browserOAuthRequired=false` / `tokenManagedBy=github-actions` を確認
5. `/api/sync`へ同期条件を送信
6. Workerがosu! API v2 `Get User`でUser / Statisticsを取得
7. 解決したUser IDで `Get User Scores / recent` を取得
8. Workerでosu! Hub用JSONへ正規化
9. Browser側でResponse再Validation
10. `id = osu:<score id>` を再生成
11. IndexedDB `results`へ保存
12. 同一Score IDは更新、手入力Resultsは維持

ユーザー名検索ではAPI v2仕様に合わせてWorker内部で `@username` 形式へ変換する。

同期フィールド:

- Score / Beatmap / Beatmapset ID
- Map / Artist / Title / Difficulty / Mapper
- Date / Timestamp
- Accuracy / Miss / Combo / PP
- Star / BPM / AR / OD / CS / HP
- MOD / Rank / Pass / Fail / Replay / Total Score
- User ID / username / avatar / country / PP / rank / profile accuracy等の公開Statistics

## 5. Token Lifecycle

Worker自身は `/oauth/token` を呼ばない。

GitHub Repository Actions Secrets:

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_TOKEN
```

`Refresh osu API Token` Workflow:

1. GitHub Actions runnerでClient Credentials Request
2. Token ResponseをValidation
3. Access Tokenを `::add-mask::` でログmask
4. GitHub Actions runnerからosu! API v2 `Get User`を実行してTokenを検証
5. `OSU_ACCESS_TOKEN`だけをCloudflare Worker Secretへ保存
6. Workerをredeploy
7. `/health` と `/api/sync` をend-to-end確認

osu! Client Credentials Tokenは24時間の有効期間を返す。更新間隔は12時間とし、有効期限より十分早く更新する。

Client SecretはCloudflare Workerへ保存しない。Access TokenはGitHub通常ファイルやGitHub Pagesへ保存しない。

## 6. External Reliability / Rate Limit

- Browser timeout: 15秒
- Worker → osu! timeout: 12秒
- `response.ok`を必ず確認
- timeout / upstream失敗時もLocalデータを削除しない
- 401をToken期限切れ / 無効として明示的に扱う
- 429を明示的に扱う
- `Retry-After`がある場合はWorker Response / Headerへ引き継ぐ

### Sync Cache

同じ `user / mode / limit / include_fails` 条件は60秒Cache APIで再利用する。

目的:

- 同一ユーザーへの短時間の再pollingを避ける
- 連打による上流Request増加を防ぐ
- APIをデータベース代わりに毎回再取得しない

### 429対策の根拠

実本番では以下を再現済み。

- Cloudflare Worker → `/oauth/token`: 429
- Cloudflare Worker → 公開Webプロフィール / Recent Scores Route: 429

そのためToken発行処理をCloudflare外のGitHub Actionsへ移した。Bearer Token付きAPI v2 Requestはosu! Web側のThrottle実装でOAuth TokenをRequest Signatureとして利用するため、Token未発行Requestの共有送信元Bucket依存を避ける目的がある。

## 7. Cloudflare Worker

公開API:

```text
GET /health
GET /api/sync?user=...&mode=osu&limit=100&include_fails=1
```

`/health`:

```json
{
  "ok": true,
  "service": "osu-hub-api",
  "apiVersion": 1,
  "configured": true,
  "upstreamMode": "api-v2-preissued-token",
  "browserOAuthRequired": false,
  "tokenManagedBy": "github-actions"
}
```

Worker Secret:

```text
OSU_ACCESS_TOKEN
```

禁止:

- `OSU_CLIENT_ID` / `OSU_CLIENT_SECRET` をWorker Runtimeへ渡す
- Workerから `/oauth/token` を呼ぶ
- SecretをGitHub通常ファイル / Pagesへ保存する
- 任意URLを中継する汎用Proxyにする

CORSは `ALLOWED_ORIGINS` で制限する。

## 8. Deployment / Operations

### Deploy osu Hub API Worker

- 手動 `workflow_dispatch`
- mainの `cloudflare/worker/**` 変更時は自動deploy
- deploy workflow自身の変更時も自動deploy
- deploy後 `/health` を検証
- 公開User ID 2で `/api/sync` を1件取得し、Worker → osu! API v2をend-to-end検証

### Refresh osu API Token

- 手動 `workflow_dispatch`
- Workflow自身のmain変更時
- `17 */12 * * *` のschedule

で実行する。

Token更新に失敗した場合は新しいTokenを保存せずWorkflowを失敗させる。既存Tokenが有効な間は利用可能だが、24時間以上更新できず401となった場合は同期をErrorにし、Local機能は維持する。

## 9. Results / Stats / Practice / Settings

- Results: osu!同期 + 手入力
- Stats: 平均ACC / 平均Miss / 最高PP / 直近ACC / MOD別
- Practice: 練習内容 / 時間 / 完了管理
- Settings: DPI / 感度 / Tablet / JSON Backup / Import

外部同期停止時もLocal機能を利用可能にする。

## 10. AI Coaching

- 複数画像
- セッション情報
- ChatGPT提出ZIP
- Schema v1 JSON取込
- AI結果はValidation後に保存
- CDN失敗時はJSON/TXT個別出力Fallback

## 11. Data / Backup

```text
DB: osuHubDB
DB Version: 1
Schema Version: 1
stores:
  results
  coaching
  practice
  settings
```

Import:

```text
parse
→ 全体Validation
→ Recovery Snapshot
→ transaction write
→ read-back verification
→ failure時rollback
```

Web 0.2.5ではIndexedDB Schema / Store / Backup Schemaを変更しない。Migration不要。

## 12. UI / Accessibility

- Navigation aria-label
- label / input関連付け
- focus-visible
- reduced-motion
- MobileでもNavigationを維持
- Loading / Empty / Error / Success表示
- Account Sync画面ではブラウザへのSecret入力不要を明示
- Token未設定 / 期限切れは利用者へ明示Error

## 13. Desktop Launcher

`osu Setup Launcher v0.17.0` を維持する。

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer
- osu!本体の自動操作はしない
- userData設定を維持
- 外部exe / Secret / 個人Pathを公開Repoへ含めない

## 14. Validation

`tests/validate-web.mjs` / `Check web` で以下を検査する。

- JS / JSON / HTML
- Version / Project Profile
- Secret混入
- Worker HTTPS / timeout
- Workerに `OSU_CLIENT_ID` / `OSU_CLIENT_SECRET` / `/oauth/token` がないこと
- WorkerがBearer `OSU_ACCESS_TOKEN` とosu! API v2を使うこと
- 60秒Cache
- 401 / 429 / Retry-After handling
- Healthのpre-issued-token mode
- Browser health compatibility guard
- deploy後 `/api/sync` E2E smoke
- 12時間Token refresh schedule / mask / direct API verification / Worker Secret upload
- Import Recovery / Rollback
- Accessibility

PRとmain pushの両方で実行する。

## 15. 完成条件

- PR CI成功
- merge後main CI成功
- GitHub Pages deploy成功
- Worker自動deploy成功
- Token refresh Workflow成功
- `/health` が `configured=true / api-v2-preissued-token`
- `/api/sync` end-to-end smoke成功
- ユーザー自身の実ブラウザAccount SyncでRecent Scores同期成功
- Results / Statsへ反映

サーバー側E2Eが成功しても実ブラウザ同期が未確認なら、その部分は未確認として記録する。

## 16. 今後

- Recent Scoresの差分 / Paging
- Best Scores同期
- 同期済みResults → AI Coaching
- BPM / ★ / AR / OD別統計
- `.osu` / `.osr`解析
- Session比較 / Goal管理
