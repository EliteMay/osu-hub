# osu! Hub 仕様書

## 1. 目的

osu!のプレイ記録、アカウント同期、AIコーチング、練習管理、設定保存、Windows補助ツールを1か所にまとめる個人向けハブ。

## 2. Project Guide / Profile

採用Guide Version: `1.2.0`

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

Source of Truth:

- Web Version: `data/site.json#siteVersion`
- Web Storage Schema: `js/storage.js#SCHEMA_VERSION`
- Desktop Version: `package.json#version`
- Desktop Update Metadata: `version.json`
- Supabase Function: `supabase/functions/osu-sync/index.ts`

## 3. システム構成

```text
GitHub Pages
   ↓ HTTPS POST
Supabase Edge Function
   ↓ Bearer Token
osu! API v2

GitHub Actions
   ↓ Client Credentials
Supabase Edge Function refresh action
   ↓
RLS保護 osu_api_tokens

Browser IndexedDB
   ↑
Results / Coaching / Practice / Settings
```

Windows固有機能はElectron製 `osu Setup Launcher` が担当する。

## 4. Account Sync

入力:

- Supabase Sync API URL
- osu! User ID / username
- Ruleset
- 同期対象 `recent | best`
- 取得件数 1〜100
- RecentのみFailを含める / 含めない
- Recent自動蓄積 有効 / 無効

ブラウザへosu! Client ID / Client Secretを入力させない。

### 4.1 Recent Plays

- osu! API v2 `Get User Scores / recent`
- UI上は `Recent Plays (24h)` と明示
- 最大100件
- Fail含有設定を使用
- 直近24時間に対象Scoreがなければ0件は正常状態
- 0件でも保存済みResultsを削除しない

### 4.2 Best Scores

- osu! API v2 `Get User Scores / best`
- 最大100件
- 古い自己ベストもResultsへ追加可能
- Fail設定は使用しない

### 4.3 自動蓄積

既定:

```text
autoSyncOnOpen = true
autoSyncMinMinutes = 5
```

Account Sync画面が開いていて可視状態のとき、最後のRecent同期から5分以上経過していればRecentを自動同期する。

- Browserを閉じている間は動作しない
- Bestは自動同期しない
- 同期中は重複実行しない
- 1分Timerで期限を確認し、5分未満ならAPIを呼ばない

### 4.4 Result保存

- `id = osu:<score id>` をBrowser側で再生成
- 同一Score IDは更新
- 新しいScore IDだけ追加
- 手入力Resultsは維持
- `note` は再同期で維持
- `syncKinds` に `recent` / `best` を集合として保持
- `lastSyncedFrom` に直近取得経路を保持

IndexedDB Schema Versionは変更しない。追加PropertyだけなのでMigration不要。

## 5. API Contract

`sync` Request例:

```json
{
  "action": "sync",
  "user": "EliteMay",
  "mode": "osu",
  "scoreType": "best",
  "limit": 100,
  "includeFails": false
}
```

`scoreType`未指定時は後方互換のため `recent`。

Response主要項目:

```json
{
  "ok": true,
  "apiVersion": 2,
  "provider": "supabase-edge-function",
  "scoreType": "recent",
  "recentWindowHours": 24,
  "syncedAt": "...",
  "user": {},
  "scores": []
}
```

`health` は `supportedScoreTypes: ["recent", "best"]` と `recentWindowHours: 24` を返す。

## 6. Token Lifecycle

GitHub Repository Actions Secrets:

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

`Refresh osu API Token` Workflow:

1. Secrets確認
2. `data/site.json`からSupabase endpoint取得
3. Edge Function `refresh`へClient ID / Secretを一時送信
4. Access TokenのみRLS保護Tableへ保存
5. `health`確認
6. Recent 1件Smoke Test
7. Best 1件Smoke Test

更新間隔は12時間。Client SecretはDBへ保存しない。Access TokenはBrowser / GitHub Pagesへ返さない。

## 7. External Reliability

- Browser timeout: 15秒
- Edge Function → osu! timeout: 12秒
- `response.ok`確認
- 401 / 429 / Retry-Afterを処理
- 同条件同期を60秒Memory Cache
- 外部同期失敗でもLocal Resultsを削除しない

Cloudflare Worker方式はBearer Token利用時も上流429を再現したため廃止済み。

## 8. Results / Stats / Practice / Settings

- Results: osu!同期 + 手入力
- Stats: 平均ACC / 平均Miss / 最高PP / 直近ACC / MOD別
- Practice: 練習内容 / 時間 / 完了管理
- Settings: DPI / 感度 / Tablet / JSON Backup / Import

外部同期停止時もLocal機能を利用可能にする。

## 9. AI Coaching

- 複数画像
- セッション情報
- ChatGPT提出ZIP
- Schema v1 JSON取込
- AI結果はValidation後に保存
- CDN失敗時はJSON/TXT個別出力Fallback

## 10. Data / Backup

```text
DB: osuHubDB
DB Version: 1
Schema Version: 1
stores:
  results
  coaching
  practice
  settings
```

Import:

```text
parse
→ 全体Validation
→ Recovery Snapshot
→ transaction write
→ read-back verification
→ failure時rollback
```

Web 0.2.7ではDB / Store / Backup Schemaを変更しない。

## 11. UI / Accessibility

- Navigation aria-label
- label / input関連付け
- focus-visible
- reduced-motion
- MobileでもNavigationを維持
- Loading / Empty / Error / Success表示
- Recent 0件をError扱いしない
- Best選択時はFail設定をdisabledにする
- Browser Secret不要を明示

## 12. Desktop Launcher

`osu Setup Launcher v0.17.0` を維持する。

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer
- osu!本体の自動操作はしない
- userData設定を維持
- 外部exe / Secret / 個人Pathを公開Repoへ含めない

## 13. Validation

`tests/validate-web.mjs` / `Check web` で以下を検査する。

- JS / JSON / HTML
- Version / Project Profile
- Secret混入
- Supabase endpoint
- Recent / Best両対応
- Recent 24h metadata
- 5分自動蓄積設定
- `syncKinds`保持
- 12時間Token refresh
- Recent / Best E2E smoke定義
- 旧Cloudflare Runtime再混入
- Import Recovery / Rollback
- Accessibility

## 14. 完成条件

- PR CI成功
- merge後main CI成功
- GitHub Pages deploy成功
- Token refresh Workflow成功
- Supabase `health`成功
- Recent Smoke成功
- Best Smoke成功
- 実ブラウザでBest同期成功
- 実ブラウザでRecent自動蓄積確認

サーバー側E2Eが成功しても実ブラウザ未確認部分は未確認として記録する。

## 15. 今後

- 24時間より前を含む全履歴同期方式の検討
- 同期済みResults → AI Coaching
- BPM / ★ / AR / OD別統計
- `.osu` / `.osr`解析
- Session比較 / Goal管理
