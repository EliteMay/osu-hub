# 作業報告書 - osu! OAuth 429根本回避

## 目的

Account Syncで `osu! OAuthのレート制限に達しました` が修正後も継続したため、`web-project-guide` Guide Version `1.2.0` の既存サイト・外部API障害対応ルートに従い、Token Cache追加ではなく通信経路そのものを再検討した。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 再調査結果

`ppy/osu-web` の現行実装を確認した。

- `/oauth/token` は `throttle` middleware対象
- Token未発行時のThrottleはOAuth Token IDではなく通常のRequest SignatureへFallbackする
- Cloudflare WorkerからのClient Credentials Token取得は共有送信元環境の影響を受ける可能性がある
- osu! Webの通常プロフィールRouteでは公開User情報をHTMLの `data-initial-data` に埋め込んでいる
- 通常Web Routeに公開Recent Scores取得経路があり、`mode / limit / offset / include_fails` を扱う

0.2.3でToken共有Cache・同時取得dedupeを追加しても429が継続したため、公開情報のみを必要とするAccount SyncではOAuth Token Endpoint自体へ依存しない構成へ変更した。

## 変更内容

### Cloudflare Worker

`cloudflare/worker/src/index.js`

- Client Credentials Token取得をAccount Syncから削除
- `OSU_CLIENT_ID` / `OSU_CLIENT_SECRET` 依存を削除
- `/oauth/token` 依存を削除
- 公開プロフィールHTMLを取得
- Cloudflare `HTMLRewriter` で `data-react="profile-page"` の `data-initial-data` を抽出
- 公開User / Statisticsを正規化
- 解決したUser IDで公開Recent Scores Web Routeを取得
- 既存Result JSON形式を維持
- 同一同期条件60秒Cacheを維持
- upstream 429 / `Retry-After` 処理を維持
- Cache Keyを `sync-v2` に変更し旧方式Cacheを混在させない

### Health / Deployment

`/health`:

```json
{
  "ok": true,
  "service": "osu-hub-api",
  "apiVersion": 1,
  "configured": true,
  "upstreamMode": "public-web",
  "oauthRequired": false
}
```

`.github/workflows/deploy-worker.yml`:

- 必須Secretを `CLOUDFLARE_ACCOUNT_ID` / `CLOUDFLARE_API_TOKEN` のみに変更
- osu! Client ID / Secretのbindingを削除
- deploy後に `public-web` / `oauthRequired=false` を検証
- main変更時の自動deployは維持

### Account Sync UI

- Worker healthが旧OAuth方式なら同期前に明示Error
- 接続成功表示を公開プロフィール経路用へ変更
- 「osu! API用Secret設定済み」の表示を削除
- Account画面でClient ID / Secret不要を明示
- 同期中表示を `osu!から最近のプレイを取得しています…` へ変更

### Validation

`tests/validate-web.mjs`:

- `HTMLRewriter`
- Profile marker
- Recent Scores path
- 60秒Cache
- 429 / Retry-After
- Workerへの `OSU_CLIENT_SECRET` / `/oauth/token` 再混入禁止
- deploy workflowへのosu! Secret再混入禁止
- `public-web / oauthRequired=false` health guard
- Account Sync UIの旧Secret案内再混入禁止

を検査する。

### Version / Docs

- Web Version: `0.2.4`
- `README.md`
- `仕様書.md`
- `CHANGELOG.md`
- `cloudflare/worker/README.md`
- `cloudflare/worker/.dev.vars.example`
- `作業報告書.md`

を現行構成へ更新した。

## 保存互換性

変更なし:

- IndexedDB DB名 `osuHubDB`
- DB Version `1`
- Object Store
- Result ID `osu:<score id>`
- Result `source: osu-api`
- Backup Schema
- Electron userData / Launcher設定

Migration不要。

## Security / Privacy

改善:

- Account Syncでosu! Client Secretを保持する必要がなくなった
- OAuth Access Tokenを発行・Cacheする必要がなくなった
- Workerは任意URLを中継せず、osu!公開プロフィールと公開Recent Scoresだけを取得する
- 個人ResultはBrowser IndexedDBへ保存し、GitHubへ自動送信しない
- Cloudflare API TokenはGitHub Actions Secretのまま公開しない

## Trade-off / 保守リスク

OAuth API v2の正式Endpointではなく、osu! Webが利用する公開Route / HTML構造へ依存する。

そのためosu!側で次が変更されると追従が必要になる。

- Public Profile URL
- `data-react="profile-page"`
- `data-initial-data`
- Recent Scores Web Route / JSON shape

想定形式が変わった場合は不正データを保存せずErrorにする。

## Verification State

### Reproduced

- 0.2.3本番Workerで実ブラウザAccount Sync時にOAuth 429が継続することを確認

### Implemented on branch

- OAuth非依存Public Web Sync
- 60秒Sync Cache
- 429 / Retry-After handling
- UI health compatibility guard
- Worker CD更新
- Static Validation更新
- Documentation更新

### PR / merge後に確認

- Pull Request `Check web`
- main `Check web`
- GitHub Pages deploy
- `Deploy osu Hub API Worker` 自動deploy
- `/health` が `public-web / oauthRequired=false`
- 実アカウントRecent Scores同期
- Results / Stats反映

### Not verified yet

- Cloudflare本番RuntimeでのHTMLRewriterによるosu! Profile抽出
- Web 0.2.4での実アカウントRecent Scores同期
- Account Sync → Results → Statsの実ブラウザE2E

未確認項目は確認済みとして扱わない。
