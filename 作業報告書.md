# 作業報告書 - Setup Launcher v0.18.5 FxSound Endpoint Active化修正

## 目的

v0.18.4実Windowsログで、SVCL側では `FxSound Speakers` を検出できる一方、Windows Core Audio FallbackのActive Endpoint一覧にはFxSoundが1件も存在せず、既定出力へ切り替えられない問題を修正する。

作業開始時に `EliteMay/web-project-guide` の最新README / START_HERE / Electron配布ルールを再確認した。最新Guideは1.9.0。既存のOne-click Update、`userData`、config schemaは変更しない。

## 実機Evidence

v0.18.4の実行ログ:

```text
matched: FxSound Speakers | FxSound Audio Enhancer\Device\FxSound Speakers\Render
Console: 2- Arctis GameBuds
Multimedia: 2- Arctis GameBuds
Communications: 2- Arctis GameBuds
Device not found: FxSound Speakers
MATCH_TOKENS=fxsound
AVAILABLE_DEVICES=Speakers (High Definition Audio Device)
  | VZ239 (NVIDIA High Definition Audio)
  | Headphones (2- Arctis GameBuds)
  | VX2458-mhd (NVIDIA High Definition Audio)
  | UG251 (NVIDIA High Definition Audio)
```

重要なのは、v0.18.4で名前Matcherが `fxsound` まで単純化されたにもかかわらず、Core Audio側の候補一覧そのものにFxSoundが存在していないこと。

## Root Cause

`src/main.js` のSVCL一覧取得は次を有効にしている。

```text
/ShowDisabledDevices 1
/ShowUnpluggedDevices 1
```

そのためSVCLはActive以外のEndpointも一覧へ含める。

一方、v0.18.4までの `tools/AudioSwitcher.cs` はCore Audioを次の条件だけで列挙していた。

```text
EnumAudioEndpoints(eRender, DeviceState.Active)
```

つまり同じPCでも、FxSound EndpointがDisabled / Unplugged / NotPresent等なら、SVCLでは「見つかる」のにCore Audio Active一覧では「存在しない」という状態が起きる。

v0.18.4実機Evidenceはこの差と一致する。現時点でFxSound Endpointの正確なStateまでは取得できていないため、v0.18.5では推測で1種類へ固定せずAll stateを診断する。

## 修正

### Core Audio

`tools/AudioSwitcher.cs` を拡張。

- `DeviceInfo.State`
- `DeviceInfo.IsActive`
- `GetRenderDevicesAll()`
- `GetRenderDeviceById()`
- Active / Disabled / NotPresent / Unpluggedを保持したまま一覧化

通常の成功確認用 `GetRenderDevices()` はActiveのみを維持する。

### FxSound準備

`tools/switch_audio_device.ps1` でFxSound targetを検出した場合、既定切替前に次を行う。

1. FxSound processが既に起動中か確認
2. 起動していなければ既知Install pathから `FxSound.exe` を起動
3. EndpointがDisabledならSVCL `/Enable` を試行
4. Core AudioでEndpointがActiveになるまで短時間Polling
5. Active化後にSVCL / PolicyConfigで既定出力を再設定
6. Multimedia default IDをread-backして成功判定

既知Install path:

```text
%ProgramFiles%\FxSound LLC\FxSound\FxSound.exe
%ProgramFiles(x86)%\FxSound LLC\FxSound\FxSound.exe
%LOCALAPPDATA%\Programs\FxSound\FxSound.exe
%LOCALAPPDATA%\FxSound\FxSound.exe
```

FxSoundの公式Forumでも通常Installerの実行ファイルは `C:\Program Files\FxSound LLC\FxSound\FxSound.exe` と案内されている。

### Diagnostics

失敗時にEndpoint名だけでなくStateまで残す。

例:

```text
SVCL_TARGET_STATE: Disabled
MATCHED_CORE_AUDIO_INACTIVE: FxSound Speakers (FxSound Audio Enhancer) [Disabled]
FXSOUND_PROCESS: started ...\FxSound.exe
SVCL_ENABLE_ATTEMPT: {endpoint-id}
```

まだActive化できない場合:

```text
Target endpoint is not active: ...
STATE=Unplugged
AVAILABLE_DEVICES=Device A [Active] | FxSound ... [Unplugged] | ...
```

次回は画面ログだけで「名前」「存在」「State」「FxSound process起動」のどこで止まったか判定できる。

## Regression Guard

PowerShell `-SelfTest` で次を確認する。

1. Active FxSound Endpointを同名のInactive候補より優先する
2. SVCL Friendly ID hintからFxSound Endpointを特定できる
3. FxSound queryがArctis Endpointへ誤一致しない

`tests/validate-audio-interop.mjs` では次を必須化する。

- Core Audio All-state列挙
- `State` / `IsActive`
- FxSound process準備
- Disabled Endpoint `/Enable`
- Inactive Endpoint診断Marker
- 既存COM IID Regression Guard

## Version / Distribution

```text
Desktop: 0.18.5
Web: 0.2.14
Config Schema: 17 (変更なし)
Updater Bootstrap: 0.18.2
```

v0.18.4からv0.18.5はアプリ内One-click Updateで取得する。

## 崩していない仕様

- Electron `userData`保存方式
- `configVersion: 17`
- SVCLを第一経路として使う方式
- Windows Core AudioをFallbackとして使う方式
- OpenTabletDriver / REAL / osu!lazer起動
- GitHub Releases + `latest.yml` + `.blockmap` のOne-click Update
- Web IndexedDB / Supabase同期

## 完了条件

PR最終Commitで以下を確認する。

- Web validation
- PowerShell 5.1 parse
- `AudioSwitcher.cs` compile
- FxSound matcher Self Test
- Audio interop regression validator
- Auto Update regression validator
- NSIS installer build
- `latest.yml` / `.blockmap`整合

main merge後にv0.18.5 Releaseと3 Artifactを確認する。

## 実Windowsで未確認

CIには実FxSound Endpointがないため、v0.18.5へOne-click Update後に実機一括実行する。

成功例:

```text
FXSOUND_PROCESS: already running ...
SVCL_TARGET_STATE: Active
MATCHED_CORE_AUDIO: FxSound Speakers (FxSound Audio Enhancer) [Active]
SWITCHED_TO: FxSound Speakers (FxSound Audio Enhancer)
VERIFIED_DEFAULT: FxSound Speakers (FxSound Audio Enhancer)
```

もしFxSound EndpointがInactiveなら、v0.18.5のState付きログを次の修正Evidenceとして使用する。
