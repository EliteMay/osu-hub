# 作業報告書 - Setup Launcher v0.18.2 One-click Update

## 目的

Setup Launcherの新版ごとにGitHub ReleaseからSetup.exeを探して手動実行する運用を改善し、v0.18.2以降はアプリ起動時に新版を確認し、`今すぐ更新`を1回押すだけでDownload / Install / Restartできるようにする。

作業開始時は`EliteMay/web-project-guide` v1.6.0のOne-click Updateルールを基準に実装した。完了確認時には最新のREADME / START_HERE / ElectronルールとGuide v1.7.0も再確認し、今回のUpdater実装へ追加変更が必要な新ルールはないことを確認した。Project側の採用Version表記は今回の実装基準である1.6.0を維持する。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## Upgrade Path

v0.18.1以前のLauncherにはAuto Updater Runtimeがないため、v0.18.2だけはSetup.exeを1回手動で導入する必要がある。

```text
v0.18.1以前
↓
v0.18.2 Setup.exeを最後の1回だけ手動Install
↓
v0.18.2以降
↓
将来VersionはLauncher内「今すぐ更新」から更新
```

## 実装内容

### Electron / Updater

- Desktop Versionを`0.18.2`へ更新
- Entry Pointを`src/bootstrap.js`へ変更
- Production dependencyへ`electron-updater 6.8.9`を追加
- GitHub Update Providerを`EliteMay/osu-hub`へ固定
- NSIS x64配布を維持
- `src/updater.js`で起動後Background Update Checkを追加
- 新Version検出時に`今すぐ更新 / あとで`を表示
- `今すぐ更新`でDownload → Install → Restart
- Download進捗をWindows taskbarへ反映
- Update失敗時は現在Versionを継続利用し、GitHub ReleasesへFallback
- Updater Logを`userData/logs/updater.log`へ保存
- Development起動では`app.isPackaged`を確認してAuto Updateを実行しない
- RendererへUpdater状態を安全に通知し、Background検出後も`今すぐ更新`Buttonを有効化

### 互換性

既存IPC名を維持した。

```text
update:check
update:open
```

既存`version.json`はWeb表示・Release Notes・旧版互換用Metadataとして残し、Electron Auto Update自体はGitHub Releaseの`latest.yml`を利用する。

変更しないもの:

- Electron `userData`保存先
- `configVersion: 17`
- FxSound等の音声デバイス設定
- OpenTabletDriver path
- REAL path
- osu!lazer path / 自動検出
- Web IndexedDB / Backup Schema
- Supabase Account Sync

Migrationは不要。

## Release Artifact

GitHub Release `v0.18.2` に同Versionで次の3 Artifactを公開した。

```text
latest.yml
osu_setup_0.18.2_setup.exe
osu_setup_0.18.2_setup.exe.blockmap
```

公開結果:

- `latest.yml`: 349 bytes
  - SHA-256 `81e1b1692b58808ff542d307ff7e0d2ca0c92c98c6dfe25e52e2994253eae9e1`
- `osu_setup_0.18.2_setup.exe`: 78,910,643 bytes
  - SHA-256 `a0ef682ba0df25c15ff237219164c69029e76d44df58dc4c76b4d35c5b2f42f3`
- `osu_setup_0.18.2_setup.exe.blockmap`: 83,447 bytes
  - SHA-256 `b6b8487f9c6acdb5253f42bcbf0e8685b03a25ebbf40dbb4e8f59dbaae65d8ef`

Release target commit:

```text
21f009062b8555ab3b8eb57e7400effbba38b4a9
```

## Security

- Update ProviderをGitHub `EliteMay/osu-hub`へ固定
- Rendererから任意Providerを設定する機能は追加しない
- electron-updater / `latest.yml`のHash / Integrityを利用
- Stable Releaseのみを対象
- Update Error時に現在Versionを維持
- Update失敗でuserDataを削除しない

現在の制限:

- Installerはコード署名なし
- Windows SmartScreenが表示される場合がある
- AuthenticodeによるPublisher検証はない

公開範囲を広げる場合はCode Signingを優先する。

## Regression Guard

`tests/validate-auto-update.mjs`で以下を確認する。

- `src/bootstrap.js`が正式Entry Point
- `electron-updater`がProduction dependency
- GitHub Providerが`EliteMay/osu-hub`
- NSIS Target維持
- `checkForUpdates` / `downloadUpdate` / `quitAndInstall`
- `今すぐ更新`導線
- fixed GitHub Releases manual fallback
- `latest.yml` / `.blockmap`のRelease Pipeline

`tests/validate-web.mjs`でもWeb表示 / Auto Update Runtime / Workflow整合を確認する。

## Verification State

### Pull Request #15

最終Head `7bd24e682e1245d937de5481ffc9ed8689690ee2` で確認済み。

- `Check web`: success
- Electron JavaScript syntax: success
- audio interop regression validator: success
- auto-update regression validator: success
- Windows PowerShell 5.1 / C# compile: success
- `npm install` / electron-updater依存解決: success
- NSIS installer build: success
- Setup.exe存在・最低サイズ: success
- `latest.yml`生成: success
- `.blockmap`生成: success
- Update Metadata / Installer Version整合: success
- PR内Release作成: skipped

PR #15はsquash merge済み。

### main merge後

Merge commit `21f009062b8555ab3b8eb57e7400effbba38b4a9` で確認済み。

- `Check web`: success
- GitHub Pages deploy: success
- Supabase token refresh: success
- Supabase health: success
- Recent Plays smoke: success
- Best Scores smoke: success
- Windows installer build: success
- audio interop regression validator: success
- auto-update regression validator: success
- Setup.exe / `latest.yml` / `.blockmap` verify: success
- GitHub Release `v0.18.2` create/upload: success

## 実Windowsで未確認

- v0.18.2 Setup.exeで既存v0.18.1から設定が維持されること
- v0.18.2起動時のUpdate確認が正常であること
- v0.18.2 → 将来Versionの実One-click Download / Install / Restart
- Auto Update後のuserData維持
- FxSound音声切替Regression
- OpenTabletDriver / REAL / osu!lazer起動Regression

Auto Updateの実機N→N+1確認にはv0.18.2より新しいReleaseが必要なため、CI成功だけで実機Update確認済みにはしない。

## Known limitation / Follow-up

- Root Electronの`package-lock.json`はまだ追跡されていない。Dependency変更の再現性向上のため、次回Dependency整備時に実package managerで生成・追跡する。
- Installer Code Signingは未導入。
