# 作業報告書 - Account SyncをSupabaseへ移行

## 目的

Cloudflare Worker経由ではosu! API v2へBearer Tokenを付けても429が継続したため、`web-project-guide` Guide Version `1.2.0` の外部Provider障害・GitHub中心運用ルールに従い、Account Syncの中継ProviderをSupabase Edge Functionsへ切り替える。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 変更理由

本番E2Eで以下を確認した。

- Cloudflare Worker `/health`: 成功
- GitHub Actions runnerからosu! OAuth / API v2: 成功
- Cloudflare Worker → osu! API v2: HTTP 429

したがってosu! Client CredentialsやToken自体の不備ではなく、Cloudflare Worker経由の上流通信が問題となる構成を廃止した。

## Supabase project

新規作成:

```text
name: osu-hub
project ref: vtnwbgejlaqpnwmlzbjy
region: ap-northeast-1
status: ACTIVE_HEALTHY
cost at creation: 0 / month
```

## Database

Migration `create_osu_api_tokens` を適用。

```text
public.osu_api_tokens
```

Security:

- RLS enabled
- anon: all privileges revoked
- authenticated: all privileges revoked
- Edge FunctionのSUPABASE_SERVICE_ROLE_KEYのみで読み書き
- Client Secretは保存しない
- Access Tokenのみ保存

GitHubにも `supabase/migrations/20260831_create_osu_api_tokens.sql` を追跡。

## Edge Function

`osu-sync` Version 1をdeploy済み。

Endpoint:

```text
https://vtnwbgejlaqpnwmlzbjy.supabase.co/functions/v1/osu-sync
```

Source:

```text
supabase/functions/osu-sync/index.ts
```

Actions:

- `health`
- `refresh`
- `sync`

CORSはGitHub Pages originとlocalhost開発環境に限定。

## Token Lifecycle

GitHub Repository Secretsとして既に登録済みの:

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

だけを使用する。

12時間ごとの `Refresh osu API Token` Workflowが:

1. `data/site.json`からSupabase endpointを取得
2. `refresh` actionへClient ID / Secretを一時送信
3. Edge Functionがosu! OAuth Tokenを発行
4. Access TokenだけをRLS保護Tableへ保存
5. `health`を確認
6. User ID 2でRecent Scores 1件E2E smoke

を実行する。

Client SecretはSupabase DBへ永続化しない。

## Browser

`js/osu-sync.js`:

- Cloudflare Worker URL処理を削除
- `endpointUrl`へ変更
- POST JSONでSupabase Edge Functionを呼ぶ
- `health` / `sync` actionを利用
- Response providerを `supabase-edge-function` としてValidation
- Client Secret処理をBrowserに追加しない
- IndexedDB保存形式は維持

`pages/account.html`:

- Cloudflare表記をSupabaseへ変更
- 接続テストButtonをProvider非依存名称へ変更
- Browser Secret不要を維持

## Cleanup

削除:

- `.github/workflows/deploy-worker.yml`
- `cloudflare/worker/**`

Cloudflare実装はGit履歴 / CHANGELOGに残るためRollback可能。

## Version

Web Version:

```text
0.2.6
```

Source of Truthは `data/site.json#siteVersion`。

## 保存互換性

変更なし:

- IndexedDB DB名 `osuHubDB`
- DB Version `1`
- Schema Version `1`
- Object Store名
- Result ID `osu:<score id>`
- Result `source: osu-api`
- Backup Schema
- Electron Launcher / userData

BrowserデータMigrationは不要。

## Verification State

### Verified

- web-project-guide 1.2.0確認
- Supabase project作成成功
- Project ACTIVE_HEALTHY
- Token Store Migration適用成功
- `osu-sync` Edge Function deploy成功
- Cloudflare → osu! API v2で429の本番再現
- GitHub Actions runner → osu! OAuth / API v2が成功することは前段で確認済み

### PR後に確認

- `Check web`
- Diff / legacy Cloudflare cleanup

### Merge後に確認

- main `Check web`
- GitHub Pages deploy
- `Refresh osu API Token` success
- Supabase `health configured=true`
- Supabase → osu! API v2 Recent Scores smoke success

### 実ブラウザで未確認

- EliteMayのRecent Scores同期
- Account Sync → IndexedDB Results反映
- Results → Stats反映

## 次

1. PR CI成功確認
2. mainへmerge
3. Token refresh E2E確認
4. Pages公開確認
5. 利用者ブラウザでEliteMay同期確認
6. 成功後、同期済みResultsをAI Coachingへ直接選択する機能へ進む
