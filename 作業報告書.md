# 作業報告書 - Setup Launcher v0.18.2 One-click Update

## 目的

Setup Launcherの新版ごとにGitHub ReleaseからSetup.exeを探して手動実行する運用を改善し、v0.18.2以降はアプリ起動時に新版を確認し、`今すぐ更新`を1回押すだけでDownload / Install / Restartできるようにする。

作業開始時に`EliteMay/web-project-guide`の最新README / START_HERE / Electronルールを確認し、共通ルール側にも継続配布Electron向けOne-click Update方針を追加した。Guideはv1.6.0としてmainへ反映済み。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 重要なUpgrade Path

v0.18.1以前のLauncherにはAuto Updater Runtimeがない。

そのため:

```text
v0.18.1以前
↓
v0.18.2 Setup.exeを最後の1回だけ手動Install
↓
v0.18.2以降
↓
将来VersionはLauncher内「今すぐ更新」から更新
```

既存版が自分自身へ後からUpdaterを追加することはできないため、v0.18.2導入時だけ手動Installが必要。

## 変更内容

### `package.json`

- Desktop Versionを`0.18.2`へ更新
- Entry Pointを`src/bootstrap.js`へ変更
- Production dependencyへ`electron-updater 6.8.9`を追加
- electron-builderのGitHub publish providerを`EliteMay/osu-hub`へ固定
- NSIS x64配布を維持

### `src/bootstrap.js`

既存Launcher RuntimeとUpdater Runtimeを明示的に読み込むEntry Pointを追加。

```text
main.js
updater.js
```

既存の音声切替・外部App起動機能を大きく書き換えず、Updater導入範囲を分離した。

### `src/updater.js`

`electron-updater`を利用して以下を実装。

1. Launcher起動後にBackgroundでUpdate確認
2. 新Versionがある場合だけ`今すぐ更新 / あとで`を表示
3. `今すぐ更新`でDownload開始
4. Download進捗をWindows taskbarへ反映
5. Download完了後に`quitAndInstall(false, true)`でInstall / Restart
6. 失敗時は現在Versionをそのまま利用
7. GitHub Releasesを開くmanual fallback
8. Updater Logを`userData/logs/updater.log`へ保存

Development起動では`app.isPackaged`を確認し、Auto Updateを実行しない。

### 既存Update UIとの互換

既存Renderer / preloadが使用しているIPC名:

```text
update:check
update:open
```

を維持し、Updater側でOne-click Updateへ接続した。

既存`version.json`はWeb表示・Release Notes・旧版互換用Metadataとして残し、Electron Auto Update自体はGitHub Releaseの`latest.yml`を利用する。

### Release Artifact

GitHub Releaseへ同Versionで次を配布する。

```text
osu_setup_<version>_setup.exe
osu_setup_<version>_setup.exe.blockmap
latest.yml
```

Windows Workflowで3 Artifactの存在を確認し、`latest.yml`が現在VersionのInstallerを参照することを検証する。

### Web / Docs

```text
Desktop: 0.18.2
Web: 0.2.11
Guide: 1.6.0
configVersion: 17（変更なし）
```

更新対象:

- `package.json`
- `src/bootstrap.js`
- `src/updater.js`
- `src/renderer/index.html`
- `data/config.json`
- `data/site.json`
- `version.json`
- `project-meta.json`
- `.github/workflows/build-windows.yml`
- `tests/validate-auto-update.mjs`
- `tests/validate-web.mjs`
- `pages/tools.html`
- `README.md`
- `CHANGELOG.md`
- `PROJECT_LEARNINGS.md`
- `作業報告書.md`

## User Data / Compatibility

変更しないもの:

- Electron `userData`保存先
- `configVersion: 17`
- FxSound等の音声デバイス設定
- OpenTabletDriver path
- REAL path
- osu!lazer path / 自動検出
- Web IndexedDB / Backup Schema
- Supabase Account Sync

Update失敗時にuserDataを削除しない。

## Security

Auto UpdateはRemoteから実行ファイルを取得するため、通常のDownload Linkより厳しく扱う。

今回:

- Update ProviderをGitHub `EliteMay/osu-hub`へ固定
- Rendererから任意Providerを設定する機能は追加しない
- electron-updater / `latest.yml`のHash / Integrityを利用
- Stable Releaseだけを対象にし、Pre-releaseを自動取得しない
- Update Error時に現在Versionを維持

現在の制限:

- Installerはコード署名なし
- Windows SmartScreenが表示される場合がある
- AuthenticodeによるPublisher検証はない

公開範囲を広げる場合はCode Signingを優先する。

## Regression Guard

`tests/validate-auto-update.mjs`を追加。

確認項目:

- `src/bootstrap.js`が正式Entry Point
- `electron-updater`がProduction dependency
- GitHub Providerが`EliteMay/osu-hub`
- NSIS Target維持
- `checkForUpdates` / `downloadUpdate` / `quitAndInstall`
- `今すぐ更新`導線
- manual Release fallback
- `latest.yml` / `.blockmap`のRelease Pipeline

`tests/validate-web.mjs`もGuide 1.6.0 / Web表示 / Auto Update Runtime / Workflow整合を確認する。

## Verification State

### Implemented / Static Source

- `web-project-guide` 1.6.0のOne-click Update共通ルール: mainへ反映済み
- osu-hubのUpdater Runtime: 実装済み
- v0.18.2 / Web 0.2.11 Metadata: 更新済み
- userData / configVersion変更なし

### PRで確認する

- Check web
- Electron JavaScript syntax
- audio interop regression validator
- auto-update regression validator
- Windows PowerShell 5.1 / C# compile
- `npm install`でelectron-updater依存解決
- NSIS installer build
- Setup.exe存在・最低サイズ
- `latest.yml`生成
- `.blockmap`生成
- Update Metadataが同Version Installerを参照
- PRではGitHub Releaseを作成しない

### main merge後に確認する

- final main `Check web`
- GitHub Pages deploy
- Supabase health / Recent / Best smoke
- Windows installer build
- GitHub Release `v0.18.2`
- Setup.exe / `latest.yml` / `.blockmap` upload

### 実Windowsで未確認

- v0.18.2 Setup.exeで既存v0.18.1から設定が維持されること
- v0.18.2起動時のUpdate確認が正常であること
- v0.18.2 → 将来Versionの実One-click Download / Install / Restart
- Auto Update後のuserData維持
- FxSound音声切替Regression
- OpenTabletDriver / REAL / osu!lazer起動Regression

Auto Updateの実機N→N+1確認にはv0.18.2より新しいReleaseが必要なため、CI成功だけで実機Update確認済みにはしない。
