# 作業報告書 - Setup Launcher v0.18.4 FxSound表示言語差修正

## 目的

v0.18.3実Windowsログで、SVCL側では `FxSound Speakers` を検出して `/SetDefault` まで実行できた一方、Windows Core Audio Fallbackが引き続き `Device not found: FxSound Speakers` で失敗した問題を修正する。

作業開始時に `EliteMay/web-project-guide` の最新README / START_HERE / Electron配布ルールを再確認した。最新Guideは1.8.0。既存のOne-click Update、`userData`、config schemaは変更しない。

## 実機Evidence

v0.18.3の実行ログ:

```text
matched: FxSound Speakers | FxSound Audio Enhancer\Device\FxSound Speakers\Render
DefaultRenderDevice*: 2- Arctis GameBuds ...
Windows標準Fallback失敗: 終了コード 1
Device not found: FxSound Speakers
```

`E_NOINTERFACE` は再発しておらず、失敗地点はCore Audioのデバイス照合。

## Root Cause

v0.18.3では語順差を吸収したが、`FxSound Speakers` の `Speakers` まで必須Tokenとして扱っていた。

Windowsが日本語表示の場合、同じEndpointが例えば次のように見える可能性がある。

```text
SVCL:        FxSound Speakers
Core Audio:  スピーカー (FxSound Audio Enhancer)
```

この場合 `FxSound` は一致するが、英語 `speakers` は日本語 `スピーカー` に一致しないためv0.18.3のMatcherでも候補から外れる。

## 修正

`tools/switch_audio_device.ps1` のToken抽出で、ProviderやWindows表示言語によって変わりやすいデバイス種別語を照合条件から除外した。

除外例:

```text
speaker / speakers
headphone / headphones
headset / earphone / earbuds
スピーカー / ヘッドホン / ヘッドセット / イヤホン
```

これにより `FxSound Speakers` は安定Token `fxsound` を使って `スピーカー (FxSound Audio Enhancer)` を候補にできる。

また未一致時の `Write-Error` に `MATCH_TOKENS` と `AVAILABLE_DEVICES` を含めるよう変更した。次回まだ失敗しても、画面に表示されるログだけでCore Audio側の実Friendly Nameを確認できる。

## Regression Guard

PowerShell `-SelfTest` を拡張し、次をWindows CIで検証する。

1. `FxSound Speakers` → `スピーカー (FxSound Audio Enhancer)` が一致
2. SVCL command-line hintからFxSound Endpointを特定できる
3. FxSound queryがArctis Endpointへ誤一致しない

`tests/validate-audio-interop.mjs` でも日本語Fixture、汎用Device Token除外、診断Markerを必須化する。

## Version / Distribution

```text
Desktop: 0.18.4
Web: 0.2.13
Config Schema: 17 (変更なし)
Updater Bootstrap: 0.18.2
```

v0.18.3からv0.18.4はアプリ内One-click Updateの実機更新確認にも利用する。

## 崩していない仕様

- Electron `userData`保存方式
- `configVersion: 17`
- SVCLを第一経路として使う方式
- Windows Core AudioをFallbackとして使う方式
- OpenTabletDriver / REAL / osu!lazer起動
- GitHub Releases + `latest.yml` + `.blockmap` のOne-click Update
- Web IndexedDB / Supabase同期

## 完了条件

PR最終Commitで以下を確認する。

- Web validation
- PowerShell 5.1 parse
- `AudioSwitcher.cs` compile
- localized FxSound matcher Self Test
- Audio interop regression validator
- Auto Update regression validator
- NSIS installer build
- `latest.yml` / `.blockmap`整合

main merge後にv0.18.4 Releaseと3 Artifactを確認する。

## 実Windowsで未確認

CIには実FxSound Endpointがないため、v0.18.4へOne-click Update後に実機一括実行する。

期待ログ:

```text
MATCHED_CORE_AUDIO: スピーカー (FxSound Audio Enhancer)
MATCH_TOKENS: fxsound
SWITCHED_TO: スピーカー (FxSound Audio Enhancer)
VERIFIED_DEFAULT: スピーカー (FxSound Audio Enhancer)
```

もしまだ未一致でも、v0.18.4ではエラー内に `AVAILABLE_DEVICES=...` が出るため、そのEvidenceから次の原因を特定できる。
