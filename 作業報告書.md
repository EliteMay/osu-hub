# 作業報告書 - Setup Launcher 音声切替確認改善

## 目的

実Windows環境で `FxSound Speakers` がSVCLの対象として一致し `/SetDefault` も実行された一方、Launcherが「既定デバイスへの変更を自動確認できない」とエラー表示した問題を修正する。

`web-project-guide` Guide Version `1.3.0` のElectron / Distributionルールを確認し、Windows固有機能は実機再確認前に成功扱いしない。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 実機で確認できた状態

利用者のv0.17.0実行ログでは以下を確認した。

- `FxSound Speakers` のSVCL照合: 成功
- SVCL `/SetDefault`: 実行
- 旧CSV Default列による自動確認: 失敗
- OpenTabletDriver.UX起動: 成功
- REAL起動: 成功

このため、主問題を「対象検索」ではなく「切替後の既定デバイス確認方法」に限定した。

## 変更内容

### Desktop 0.18.0

`src/main.js`:

1. SVCLで対象を検索
2. `/SetDefault <target> all` を実行
3. `DefaultRenderDevice` / `DefaultRenderDeviceMulti` / `DefaultRenderDeviceComm` を `/GetColumnValue` で参照
4. 対象Name / Command-Line Friendly ID / Item IDと直接照合
5. 上記確認が使えない場合は従来CSV Default列へFallback
6. それでも確認できない場合はWindows PolicyConfigのPowerShell Fallbackを実行
7. Fallbackでも確認できない場合だけエラー扱い

`tools/switch_audio_device.ps1`:

- PolicyConfigで3 roleを切替
- 250ms待機
- Multimedia default deviceを再取得
- 対象Device IDと一致した場合だけexit 0
- 不一致は明示的に失敗

### Version

```text
Desktop: 0.18.0
Web: 0.2.9
```

既存 `configVersion` は17のままで、保存Schema変更はない。

## CI / Release

`.github/workflows/build-windows.yml` を更新。

- Desktop関連Pull RequestでもWindows buildを実行
- PRではReleaseを作成しない
- `node --check` でmain / preload / rendererをbuild前に検証
- installer生成・最低サイズ確認
- main merge後のみ `v<package version>` Releaseを作成
- Release Notesは `version.json#releaseNotes` を使用

## Compatibility

維持するもの:

- Electron userData設定
- `configVersion: 17`
- 既存のFxSound等の音声デバイス名
- OpenTabletDriver path
- REAL path
- osu!lazer path / 自動検出
- Web IndexedDB / Backup Schema
- Supabase Account Sync

Migrationは不要。

## Verification State

### Confirmed before PR

- web-project-guide 1.3.0を再確認
- v0.17.0実機ログでSVCL対象照合成功
- v0.17.0実機ログでOpenTabletDriver起動成功
- v0.17.0実機ログでREAL起動成功
- NirSoft公式仕様で `/SetDefault ... all`、`DefaultRenderDevice*`、`/GetColumnValue` を確認

### PRで確認する

- `Check web`
- Windows installer PR build
- `node --check src/main.js`
- Setup.exe生成

### merge後に確認する

- main `Check web`
- Windows installer build
- GitHub Release `v0.18.0`
- `osu_setup_0.18.0_setup.exe` upload
- GitHub Pages 0.2.9 deploy

### 実Windowsで再確認が必要

- FxSound Speakersへの実切替
- Launcher上で音声切替がsuccess表示になること
- OpenTabletDriver / REAL / osu!lazerの一括起動Regression
- update install後に既存userData設定が維持されること

未確認項目は確認済みとして扱わない。

## 次

1. PR CI / Windows buildを通す
2. main merge後にv0.18.0 Releaseを作成
3. 利用者Windowsへv0.18.0をinstall
4. 同じ `FxSound Speakers` 設定で再実行
5. 実ログを確認してWindows固有動作を確定する
