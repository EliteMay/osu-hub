# 作業報告書 - osu! Hub v0.2.0

## 今回変更した内容

osu! Hubへ `Account Sync` を追加し、GitHub PagesからCloudflare Worker経由でosu!api v2のRecent Scoresを取得する構成へ拡張しました。

既存のAI Coaching、Results、Stats、Practice、Settings、Desktop Launcherは削除していません。

## 追加した機能

### Account Sync

- `pages/account.html`を追加
- Cloudflare Worker URLを保存
- osu! User ID / ユーザー名を保存
- Ruleset選択
- Recent Scores 1〜100件取得
- Failスコアを含める設定
- Worker接続テスト
- 同期結果プレビュー
- osu!プロフィールのPP / Rank / Accuracy表示
- 最終同期日時保存

### Results連携

osu! APIから取得したスコアを既存`results` Object Storeへ保存します。

- `id = osu:<score id>`で重複防止
- 新規 / 更新件数を分けて表示
- 再同期時に既存スコアを更新
- 手入力Resultsは削除しない
- API同期済みResultへ付けた`note`は再同期時に維持

### Cloudflare Worker

追加:

```text
cloudflare/worker/src/index.js
cloudflare/worker/wrangler.toml
cloudflare/worker/package.json
cloudflare/worker/.dev.vars.example
cloudflare/worker/README.md
```

Worker機能:

- osu! Client Credentials Grant
- OAuthアクセストークンの短期キャッシュ
- `GET /health`
- `GET /api/sync`
- User ID / ユーザー名の解決
- Recent Scores取得
- osu! Hub用Results形式への正規化
- `ALLOWED_ORIGINS`によるブラウザOrigin制限
- 取得件数を最大100件に制限
- 任意のosu! API URLを中継する汎用Proxyにはしていない

## Secret管理

Cloudflare側へ保存する値:

```text
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

`.gitignore`へ以下を追加しました。

- `.dev.vars`
- `.dev.vars.*`
- `.env`
- `.env.*`
- `.wrangler/`

ExampleファイルのみGit管理対象です。

## 更新した画面

- HomeへAccount Syncカード / ボタン追加
- Resultsへ`osu!から同期`導線追加
- Statsへ同期導線追加
- SettingsへAccount Sync設定導線追加
- AI Coaching / Practice / Desktop Toolsを含むナビゲーションへAccount Sync追加
- Web表示バージョンをv0.2.0へ更新

## 更新したデータ / CI

### `data/site.json`

- `siteVersion: 0.2.0`
- `osuApi`設定を追加
- Worker URLは本番deploy前のため空欄

### `.github/workflows/check-web.yml`

追加チェック:

- `js/osu-sync.js`
- `cloudflare/worker/src/index.js`
- `cloudflare/worker/package.json`

`cloudflare/worker/**`変更時にもCIが動くようにしました。

## 変更した主なファイル

### 新規

- `pages/account.html`
- `js/osu-sync.js`
- `cloudflare/worker/src/index.js`
- `cloudflare/worker/wrangler.toml`
- `cloudflare/worker/package.json`
- `cloudflare/worker/.dev.vars.example`
- `cloudflare/worker/README.md`

### 更新

- `index.html`
- `pages/coaching.html`
- `pages/results.html`
- `pages/practice.html`
- `pages/stats.html`
- `pages/settings.html`
- `pages/tools.html`
- `data/site.json`
- `.gitignore`
- `.github/workflows/check-web.yml`
- `README.md`
- `仕様書.md`
- `作業報告書.md`

## 崩してはいけない仕様

### Web / API

- osu! Client SecretをGitHub / GitHub Pagesへ置かない
- 個人プレイデータをGitHubへ自動送信しない
- APIが止まっても既存ローカル機能を壊さない
- 手入力ResultsをAPI同期で削除しない
- 同一osu! Score IDの重複を増やさない
- IndexedDBとJSONバックアップを維持する
- AI Coachingに有料APIを必須化しない

### Desktop

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer の一括起動
- ユーザー設定を更新時に維持する
- osu!本体の自動操作・プレイ補助を行わない
- 外部ツール本体や個人PC固有パスを公開リポジトリへ直接含めない

## 確認済み

- `EliteMay/osu-hub`へ新規コードを反映できること
- 既存Web / Electronファイルを削除していないこと
- osu! APIのClient Credentials Grantが`public`スコープを利用できる仕様であること
- Recent Scoresが`GET /users/{user}/scores/recent`で取得でき、`include_fails=1`が利用できる仕様であること
- Cloudflare Worker Secretを公開変数とは分けて保存できる構成にしていること
- GitHub Pages公開対象へWorkerソースやSecretファイルを含めていないこと

## 未確認

- Cloudflare Workerの本番deploy
- Cloudflareへの`OSU_CLIENT_ID` / `OSU_CLIENT_SECRET`設定
- 実osu!アカウントでRecent Scoresが返ること
- 実ブラウザでAccount Sync → Results → Statsが一連で動くこと
- Cloudflare Workerの本番URLを`data/site.json`へ設定すること
- API同期データをAI Coachingへ直接含める機能
- Windows実機でのDesktop Launcher音声切替問題の完全解消
- GitHub ReleasesのSetup.exe初回配布

未確認項目は確認済みとして扱いません。

## 今後必要な作業

1. CloudflareへWorkerをdeploy
2. osu! OAuth Applicationを作成
3. Client ID / SecretをCloudflare Secretへ登録
4. Worker URLをAccount Syncへ設定
5. 実アカウントでRecent Scoresを同期
6. Results / Statsの値を実データで確認
7. Worker URLを`data/site.json`の既定値へ反映
8. 同期済みResultsをAI Coaching提出ZIPへ直接含める
9. Best Scores / 過去履歴の差分同期を追加
