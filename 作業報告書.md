# 作業報告書 - Recent / Best同期と自動蓄積

## 目的

Supabase移行後のAccount Syncを実用段階へ進めるため、`web-project-guide` Guide Version `1.2.0` を基準に以下を追加する。

- Recent Playsが直近24時間であることをUIへ明示
- Best Scores上位100件同期
- Recentの自動蓄積
- 同一Score IDの重複防止を維持
- Recent / Best両経路の本番Smoke Test

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 要件

### Recent

- `scoreType=recent`
- 最大100件
- 直近24時間
- Fail含有設定あり
- 0件はErrorではない

### Best

- `scoreType=best`
- 最大100件
- Fail設定は使用しない

### 自動蓄積

- 既定ON
- Account Sync画面が開いている間だけ動作
- 最後のRecent同期から5分以上経過した場合のみ同期
- Bestは自動同期しない
- Browserを閉じている間は動作しない

## Supabase Edge Function

`supabase/functions/osu-sync/index.ts` をAPI Version 2へ更新。

追加:

- `ALLOWED_SCORE_TYPES = recent / best`
- Request `scoreType`
- `GET /scores/recent` と `GET /scores/best` の切替
- Cache KeyへscoreTypeを追加
- `health.supportedScoreTypes`
- `health.recentWindowHours = 24`

後方互換:

- `scoreType`未指定は `recent`

Supabase本番Function Version 2へdeploy済み。

## Browser

`js/osu-sync.js`:

- Recent / Best切替
- Best時のFail設定disabled
- Recent 0件専用メッセージ
- Recent / Best最終同期時刻
- `syncKinds` / `lastSyncedFrom`
- 5分Recent自動同期
- 可視状態のみ自動同期
- 同期中の重複実行防止

`pages/account.html`:

- Recent Plays (24h)を明示
- Best Scores選択追加
- 自動蓄積設定追加
- 蓄積ルール説明追加

## Data / Compatibility

変更なし:

- IndexedDB DB名 `osuHubDB`
- DB Version `1`
- Schema Version `1`
- Object Store名
- Result ID `osu:<score id>`
- Backup Schema
- Electron Launcher / userData

Result Objectへ追加するProperty:

```text
syncKinds: [recent, best]
lastSyncedFrom: recent | best
```

Property追加だけなのでMigration不要。既存Resultの`note`は再同期で維持する。

## Config / Version

`data/site.json`:

```text
Web Version: 0.2.7
scoreTypes: recent / best
recentWindowHours: 24
autoSyncOnOpen: true
autoSyncMinMinutes: 5
```

## CI / Operations

`Refresh osu API Token` Workflowを更新。

merge後に以下を実行する。

1. Token refresh
2. Supabase health
3. Recent Plays 1件Smoke
4. Best Scores 1件Smoke

`tests/validate-web.mjs`へRecent / Best / 24h / 自動蓄積 / syncKindsのStatic Guardを追加した。

## Security

変更なし:

- BrowserへClient Secretを渡さない
- Client SecretをSupabase DBへ保存しない
- Access TokenをBrowserへ返さない
- External ResponseをBrowser側でもValidation
- 旧Cloudflare Runtimeを復活させない

## Verification State

### Verified

- web-project-guide 1.2.0確認
- Supabase Edge Function Version 2 deploy成功
- 既存Token Store / Provider構成を維持

### PR後に確認

- `Check web`
- Diff /保存互換性

### Merge後に確認

- main `Check web`
- GitHub Pages deploy
- Refresh osu API Token success
- Supabase health success
- Recent Plays smoke success
- Best Scores smoke success

### 実ブラウザで未確認

- Best ScoresをEliteMayで同期してResultsへ追加
- Recent自動蓄積を5分以上開いた状態で確認
- Results / Statsへの追加分反映

未確認項目は確認済みとして扱わない。

## 次

1. PR CI
2. mainへmerge
3. Recent / BestサーバーE2E確認
4. 利用者ブラウザでBest同期確認
5. 自動蓄積確認
6. 同期済みResultsをAI Coachingへ直接選択する機能へ進む
