# 作業報告書 - osu! Hub Web v0.2.1 Guide整合

## 今回の目的

`EliteMay/web-project-guide` Guide Version `1.1.1` を基準に、osu! Hubの既存実装を監査し、既存のIndexedDBデータ構造・Account Sync・AI Coaching・Desktop Launcherを壊さずに安全性、操作性、検証性を改善する。

今回のProject Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 変更した内容

### Project Metadata / Source of Truth

追加:

- `project-meta.json`

記録内容:

- Guide Version `1.1.1`
- Project Profile
- Web Version / Storage Schema / Desktop VersionのSource of Truth

Web Versionは `data/site.json#siteVersion` を正本とし、各HTMLのVersion直書きを削除して `js/site-meta.js` から表示する構成へ変更した。

### UI / Responsive / Accessibility

- 900px以下でNavigationを `display:none` にしていた構造を廃止
- MobileではNavigationを横Scrollして利用可能に変更
- `:focus-visible` を追加
- `prefers-reduced-motion` 対応を追加
- `nav` に `aria-label` を追加
- Form `label` とInputを `for` / `id` で関連付け
- AI CoachingのDropzoneをクリック専用div相当からbuttonへ変更
- AI Coaching画像削除Buttonの操作領域を拡大
- Account Sync statusへ `role=status` / `aria-live` を追加
- HomeのAccount SyncをWorker未設定時 `SETUP REQUIRED` と表示
- 未実装Stats / Desktop候補を「開発予定 / 未実装」と明示

### IndexedDB / Backup

`js/storage.js` を改善。

- `SCHEMA_VERSION = 1` を明示
- Import PayloadのSchema Validation
- Store型Validation
- `id` / `key` Validation
- Store内の重複ID検出
- `source=osu-api` のResultについて `osuScoreId` と `id=osu:<score id>` の整合性確認
- 全Storeを検証後に書き込み開始
- 複数Storeを1 IndexedDB transactionで反映
- transaction失敗時に途中状態をcommitしない
- 通常の `put/remove/clear` もtransaction完了後に成功扱いする

既存DB名 `osuHubDB`、DB Version `1`、Store名は変更していないためMigrationは不要。

### Account Sync / Browser

`js/osu-sync.js` を改善。

- Production Worker URLはHTTPS必須
- localhost / 127.0.0.1のみHTTP許可
- Browser Request timeout 15秒
- Worker `/health` Response Validation
- `/api/sync` Response Validation
- Browser側でもScore IDを `osu:<osuScoreId>` から再生成
- Workerから返ったObjectをそのまま永続化せず、許可FieldへNormalize
- Accuracy / Miss / User ID等の基本Validation
- URL FieldをHTTP(S)に制限
- Error / timeout時の修正可能なMessage
- 設定保存失敗をToastで表示

### Cloudflare Worker

`cloudflare/worker/src/index.js` を改善。

- osu! OAuth / API上流通信timeout 12秒
- Network / timeout Errorを明示
- 401時Token Cacheを破棄して1回だけ再認証
- osu! User / Score ID / Recent Scores Response型をValidation
- 文字列長をNormalize
- Recent Scoresをrequested limit以下に制限
- 既存のOrigin制限、Secret分離、限定Endpoint方針は維持

### AI Coaching

- ChatGPT返却JSONの `schemaVersion` をValidation
- `strengths` / `weaknesses` / `patterns` / `nextGoals` / `confidenceNotes` を文字列配列としてValidation
- `recommendedPractice` のObject形式をValidation
- 不正JSONは保存しない
- 画像1枚20MB、最大100枚の上限を追加
- Object URLをload/error後に解放
- Coaching履歴削除前に確認
- JSZip CDN失敗時のJSON/TXT Fallbackを維持

### Results / Practice / Settings

- Result手動保存に `source: manual` を追加
- Result削除失敗を表示
- Practice削除前確認を追加
- Practice更新・保存・削除失敗を表示
- Settings保存失敗を表示
- Backup Export失敗を表示
- Backup Import前にValidationし、失敗時は既存データを変更しない

### CI / Final-state Validation

追加:

- `tests/validate-web.mjs`

更新:

- `.github/workflows/check-web.yml`

PRとmain pushの両方で以下を確認する構成へ変更。

- JS / MJS syntax
- JSON syntax
- HTML local reference
- HTML duplicate ID
- `label for`
- Navigation `aria-label`
- Web Version hardcode再混入
- Guide Version / Project Profile
- Desktop Version整合
- Production Worker HTTPS
- request timeout設定範囲
- Mobile Navigation非表示の再混入
- `focus-visible`
- reduced-motion
- 公開WebへのSecret値混入
- `.env` / `.dev.vars`誤追跡

## 変更した主なファイル

### 新規

- `project-meta.json`
- `js/site-meta.js`
- `tests/validate-web.mjs`

### 更新

- `index.html`
- `pages/account.html`
- `pages/coaching.html`
- `pages/results.html`
- `pages/practice.html`
- `pages/stats.html`
- `pages/settings.html`
- `pages/tools.html`
- `css/styles.css`
- `js/storage.js`
- `js/app.js`
- `js/osu-sync.js`
- `data/site.json`
- `cloudflare/worker/src/index.js`
- `.github/workflows/check-web.yml`
- `README.md`
- `仕様書.md`
- `作業報告書.md`

## 保存・互換性への影響

### 維持

- IndexedDB DB名: `osuHubDB`
- IndexedDB DB Version: `1`
- Object Stores: `results`, `coaching`, `practice`, `settings`
- Account Sync Key: `osu:<score id>`
- 手入力ResultsをAccount Syncで削除しない仕様
- API Resultの手動 `note` を再同期で維持する仕様
- Desktop Launcherの設定保存先 / Electron本体処理

### 変更

- 新しい手入力Resultには `source: manual` を追加
- Backup Importは以前より厳密なValidationを行う
- 不正なBackup / AI JSONは拒否する

既存正常データを破壊するSchema変更は行っていない。

## Electron / Desktopについて

今回、Windows固有のElectron起動処理は変更していない。

理由:

- 音声デバイス切替
- 外部exe起動
- Setup.exe
- Windows Path

は実Windows確認が必要であり、Guide上もStatic Validationだけで確認済み扱いにしないため。

`debug_info.bat` 等のDesktop troubleshooting資産は一時Patchではなく既存Launcherの診断用途として残している。

## Static Validation予定 / 状態

この変更はBranch / Pull Request経由で適用し、PR CI成功後にmergeする。

merge後は最終main Commitで以下を再確認する。

- `Check web`
- `Deploy osu Hub Pages`
- 最終mainのCommit SHA
- 公開Pages ArtifactがWebファイルだけであること

CI成功だけで実ブラウザ・実Windows確認済みとは扱わない。

## 未確認

- Cloudflare Worker本番deploy
- `OSU_CLIENT_ID` / `OSU_CLIENT_SECRET` の本番Secret設定
- 実osu!アカウントRecent Scores同期
- Account Sync → Results → Statsの実ブラウザE2E
- Firefox / Chromiumでの今回変更後の主要操作
- 320px相当Small ViewportのHuman visual確認
- AI Coachingで実画像を大量投入した長時間利用
- Windows実機のSetup Launcher音声切替
- Setup.exe build / install / uninstall
- GitHub Release初回配布

## Known limitation

Root ElectronとCloudflare Workerの `package-lock.json` は現在Git管理されていない。

今回dependency自体は変更していないため、lock fileを推測で生成していない。次回 `npm install` / dependency updateを実行する際に実際のpackage managerで生成し、commitする。

## 次に必要な作業

1. Pull Request CIを通す
2. PRをmainへmerge
3. 最終mainでCI / Pages deployを確認
4. Cloudflare Workerを本番deploy
5. osu! OAuth SecretをCloudflareへ登録
6. 実アカウントでRecent Scores同期
7. 実ブラウザでResults / Stats反映を確認
8. 同期済みResultsをAI Coachingへ直接含める機能を実装
