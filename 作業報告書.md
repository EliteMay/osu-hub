# 作業報告書 - Web 0.2.18 Visual Refresh

## 目的

osu! HubのWeb全体が、巨大Hero・Card・Gradientに寄りすぎた汎用的な見た目になっていたため、機能やデータを主役にした **osu! performance workspace** 方向へ整理する。

機能・保存形式・Supabase・Electron Launcherには触れず、Navigation / Hierarchy / Typography / Spacing / Surface / Responsiveを中心に改善する。

作業開始時に `EliteMay/web-project-guide` の最新 `README.md` / `START_HERE.md` / `docs/04-ui-ux-accessibility.md` / `docs/17-visual-quality-baseline.md` を確認した。確認時点のGuide Versionは **1.11.0**。

## Design Direction

### Concept

`osu! performance workspace / compact product UI`

- osu!のPink AccentはIdentityとして残す
- PinkはPrimary Action / Active /重要Statusへ限定
- GradientやGlowを主役にせず、Border / Spacing / Background differenceでHierarchyを作る
- ホームはMarketing Landing Pageではなく、次の作業へ移動するWorkspace入口として扱う
- Data / Sync / Coaching / Practice / Desktop Toolsの役割を視覚的に分ける
- 背景の小さいDot Patternをbeatmap fieldの弱いSignatureとして使う

## 変更

### Global Visual System

`css/styles.css`を整理した。

- 背景をNeutral dark中心へ変更
- Sticky topbarをコンパクトなProduct navigationへ変更
- Brand circleをGradient依存からOutline / inset ringへ変更
- Active navigationをPink accent + Borderで明示
- Radius / Border / Surface / Text colorをToken化して揺れを削減
- Primary ButtonのGradientをやめて単色Accentへ変更
- Panel / Form / Data RowのShadow依存を減らす
- Focus-visible / prefers-reduced-motionを維持

### Home

`index.html`をWorkspace入口として再構成した。

- Hero copyを短くし、Primary Actionを「プレイ結果を同期」へ明確化
- 古いCloudflare Worker表記を削除し、現行のSupabase Edge Functionへ修正
- Local-first / Supabase / Desktop Launcherの構成を右側Status Panelへ整理
- Metricsを独立Card 4枚ではなく1本のData Stripとして表示
- 主要機能を12-column Gridへ変更
  - Account Sync / AI Coachingを強める
  - Results / Stats / Practiceを中密度
  - Player Settings / Desktop Toolsを下段でまとめる
- CTA文言を「開く」だけでなく実際のActionへ変更

### Shared Pages

既存HTMLのClass契約を維持したまま、全ページへ次を反映する。

- Page Headに明確なDividerと読みやすいLine length
- Form Input / Select / Textareaの高さ・Focus・Hoverを統一
- Data listをCardの集合からTable-like rowへ寄せる
- Notice / Success / Calloutの意味をBackground / Border / Textで分離
- Chart / Practice row / Preview / ToastのVisual languageを共通化

### Responsive

- 980px以下でNavigationを横スクロール可能な2段Headerへ変更
- Hero / Splitを1Columnへ変更
- 720px以下でFeature / Formを1Columnへ変更
- Metricsは4 → 2 → 1列へ段階的に変更
- Data RowはMobileで主要情報を上段へ逃がす
- 430px以下ではPrimary ActionをFull widthへ変更

## Version

```text
Web: 0.2.18
Desktop: 0.18.9 (変更なし)
Web Storage Schema: 1 (変更なし)
Guide checked: 1.11.0
```

## 崩していない仕様

- IndexedDB保存形式 / Import / Recovery
- Supabase Edge Functionとosu! API同期
- Recent / Bestの同期仕様
- AI Coaching JSON Schema
- Electron Launcher / One-click Update
- GitHub Pagesの相対Path
- Navigation link / DOM ID / JavaScript契約

## Visual Verification

ローカルで最終CSS + Home HTMLを1440 x 1100相当のScreenshotとして確認した。

確認済み:

- First view hierarchy
- Top navigation
- Hero / Primary Action
- Metric strip
- Feature grid
- Typography / spacing / border consistency
- 1440pxでのOverflowなし

Headless Chromiumは実行環境のDBus関連でScreenshot生成が安定せず、代替Rendererで確認した。したがって **実GitHub PagesのBrowser renderingはmain merge / Pages deploy後も最終確認対象** とする。

## 完了条件

- `tests/validate-web.mjs` 成功
- HTML / JavaScript syntax既存CI成功
- GitHub Pages build / deploy成功
- 既存機能・Storage・API契約を変更していないこと
- Main merge後の公開Pagesで重大なLayout崩れがないこと
