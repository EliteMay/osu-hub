# 作業報告書 - Cloudflare共有送信元429の根本回避

## 目的

Account Syncでosu!の429が継続したため、`web-project-guide` Guide Version `1.2.0` の既存サイト・外部API障害対応ルートに従い、Cloudflare Worker自身からToken発行や公開Web取得を行う構成をやめ、同期Tokenの発行場所をCloudflare外へ移した。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 障害の再現

### 0.2.3

Cloudflare WorkerからClient Credentials `/oauth/token` を利用。

- Token Cache / dedupeを追加しても実ブラウザで429継続

### 0.2.4

OAuth Token Endpointを避け、osu!公開プロフィールHTMLと公開Recent Scores Web Routeへ切替。

本番deploy後に `/health` だけでなく `/api/sync` を実行するend-to-end smoke testを追加した結果:

- Worker `/health`: 成功
- Worker deploy: 成功
- Worker `/api/sync`: HTTP 429

公開Web経路でもCloudflare Worker → osu!通信がRate Limit対象になることを再現した。

## 原因判断

`ppy/osu-web` の現行 `ThrottleRequests` 実装を確認した。

- OAuth Tokenが存在するRequestはToken KeyをRequest Signatureへ利用
- TokenがないRequestは通常のRequest SignatureへFallback

このため、Cloudflare共有送信元からToken未発行状態のRequestを繰り返す構成より、Cloudflare外で発行済みBearer Tokenを用意してAPI v2を使う構成が適切と判断した。

## 採用構成

```text
GitHub Actions
  └─ OSU_CLIENT_ID / OSU_CLIENT_SECRET
       ↓ Client Credentials
osu! /oauth/token
       ↓ short-lived Access Token
GitHub Actionsでmask / API v2検証
       ↓
Cloudflare Worker Secret: OSU_ACCESS_TOKEN
       ↓ Bearer Token
osu! API v2
```

### Security方針

- `OSU_CLIENT_ID` / `OSU_CLIENT_SECRET` はGitHub Actions Secretsだけで管理
- Client SecretをCloudflare Workerへ保存しない
- Worker Runtimeから `/oauth/token` を呼ばない
- Access TokenをGitHub通常ファイルへ保存しない
- 発行Access TokenはActionsログでmask
- Cloudflareには短期 `OSU_ACCESS_TOKEN` だけを保存

## Stage 1 - Token発行経路の事前検証

### 変更

`.github/workflows/refresh-osu-token.yml` を追加。

- GitHub Actions runnerからClient Credentials Tokenを発行
- Token ResponseをJSON Validation
- Access Tokenを `::add-mask::`
- GitHub Actions runnerからosu! API v2 `Get User`を実行
- 成功したTokenだけを `OSU_ACCESS_TOKEN` Worker Secretへ保存
- 12時間ごとのscheduleを設定

### Verified

PR #7を経由してmainへ反映。

GitHub Actions Run `33313361083` で次を確認済み。

- 必須GitHub Secrets: OK
- GitHub Actions runner → `/oauth/token`: 成功
- 発行Token → osu! API v2実認証: 成功
- `OSU_ACCESS_TOKEN` → Cloudflare Worker Secret保存: 成功
- Workflow全体: success

これにより「Cloudflare外ならToken発行できる」「発行TokenでAPI v2を利用できる」「Worker Secretへ安全に渡せる」の3点を本番環境で確認した。

## Stage 2 - Account Sync本体の切替

### Cloudflare Worker

`cloudflare/worker/src/index.js`

- 公開HTML / Web Route取得を削除
- `OSU_ACCESS_TOKEN` をBearer Tokenとしてosu! API v2へ送信
- `Get User`でユーザー・Statistics取得
- `Get User Scores / recent`でRecent Scores取得
- ユーザー名は `@username` 形式へ変換
- 既存Result正規化形式を維持
- Cache Keyを `sync-v3` へ変更
- 60秒Sync Cacheを維持
- 401 / 429 / Retry-After / timeout処理
- WorkerにTokenがない場合は503

新Health:

```json
{
  "ok": true,
  "service": "osu-hub-api",
  "apiVersion": 1,
  "configured": true,
  "upstreamMode": "api-v2-preissued-token",
  "browserOAuthRequired": false,
  "tokenManagedBy": "github-actions"
}
```

### Deploy Workflow

`.github/workflows/deploy-worker.yml`

- Worker code deploy後に新HealthをValidation
- 公開User ID 2で `/api/sync` を1件実行
- Worker → osu! API v2までend-to-endで確認
- 通常deploy Workflowはosu! Client ID / Secretを要求しない

### Token Refresh Workflow

`.github/workflows/refresh-osu-token.yml`

- 12時間ごとにToken更新
- Token直接検証後にWorker Secretへ保存
- Worker redeploy後に `/health` を確認
- `/api/sync` まで再実行し、Token更新後のend-to-endを確認

### Browser / UI

`js/osu-sync.js`

- 新Health形式を必須化
- `configured=false` を同期Token未設定として明示
- `api-v2-preissued-token` / `browserOAuthRequired=false` / `tokenManagedBy=github-actions` を確認
- 接続成功表示をAPI v2方式へ変更

`pages/account.html`

- ブラウザへのClient ID / Client Secret入力不要を明示
- GitHub Actionsが短期Tokenを管理することを説明

### Validation

`tests/validate-web.mjs`

- WorkerにClient Credentials / `/oauth/token` が再混入していないこと
- `OSU_ACCESS_TOKEN` / Bearer / API v2利用
- 401 / 429 / Retry-After
- 60秒Cache
- 新Health mode
- Browser health guard
- deploy E2E smoke
- Token refresh 12時間schedule
- Token mask
- GitHub Actions runnerでのAPI v2直接検証
- Worker Secret upload

を静的検査する。

## Version / Docs

Web Versionを `0.2.5` へ更新。

更新対象:

- `data/site.json`
- `README.md`
- `仕様書.md`
- `CHANGELOG.md`
- `cloudflare/worker/README.md`
- `cloudflare/worker/.dev.vars.example`
- `作業報告書.md`

## 保存互換性

変更なし:

- IndexedDB DB名 `osuHubDB`
- DB Version `1`
- Schema Version `1`
- Object Store
- Result ID `osu:<score id>`
- Result `source: osu-api`
- Backup Schema
- Electron Launcher / userData

Migration不要。

## Reliability / Fallback

Client Credentials Tokenは24時間の有効期間を返すため、12時間ごとに更新する。

Token更新が失敗した場合:

- 新しい無効値でWorker Secretを上書きしない
- 既存Tokenが有効な間は同期継続可能
- 24時間以上更新できず401になった場合は同期をErrorにする
- 既存IndexedDB Resultsは削除しない
- LocalのResults / Coaching / Practice / Settingsは利用可能

## Verification State

### Verified before Stage 2 merge

- 公開Web方式でも本番 `/api/sync` 429を再現
- GitHub Actions runnerでClient Credentials Token発行成功
- 発行Tokenでosu! API v2実認証成功
- `OSU_ACCESS_TOKEN` Worker Secret保存成功
- Stage 1 Workflow Run `33313361083`: success

### Stage 2 branch implemented

- Worker API v2 pre-issued-token化
- Browser health guard更新
- Token refresh後E2E verification追加
- Worker deploy E2E verification更新
- Static Validation更新
- Web Version 0.2.5
- Documentation更新

### Merge後に最終確認する項目

- Stage 2 PR `Check web`
- merge後main `Check web`
- GitHub Pages deploy
- `Deploy osu Hub API Worker`
- `/health` `configured=true / api-v2-preissued-token`
- `/api/sync` end-to-end smoke
- `Refresh osu API Token` のToken更新後E2E

### 実ブラウザで未確認

- ユーザー自身のAccount SyncでRecent Scores取得
- Account Sync → IndexedDB Results → Stats反映

サーバー側E2Eが成功しても、実ブラウザ項目は確認済み扱いしない。
