# 作業報告書 - osu! Hub v0.1.0

## 今回変更した内容

従来の `osu Setup Launcher` 単体リポジトリから、osu!関連機能をまとめる `osu! Hub` へ方針変更しました。

既存Electron Launcherは削除・大幅変更せず残し、Web側を新規追加しています。

### 追加したWeb機能

- `index.html` ホーム
- AI Coaching
  - 複数リザルト画像ドラッグ&ドロップ
  - セッション情報入力
  - ChatGPT提出用ZIP生成
  - `prompt.txt`
  - `coaching_manifest.json`
  - `notes.txt`
  - リザルト画像同梱
  - ChatGPT返却JSON取込
  - 分析履歴保存
- Results
  - 譜面名 / 日付 / MOD / ACC / Miss / Combo / PP / ★ / BPM / メモ
  - IndexedDB保存
  - 一覧 / 削除
- Stats
  - 登録数
  - 平均ACC
  - 平均Miss
  - 最高PP
  - 直近ACC表示
  - MOD別件数 / 平均ACC
- Practice
  - カテゴリ / 内容 / 時間 / メモ
  - 完了チェック
  - 削除
- Settings
  - プレイヤー名
  - モード
  - DPI / 感度
  - Tablet Area / Offset
  - JSONバックアップ / 復元
- Desktop Tools
  - Setup Launcher説明
  - GitHub Releasesへの導線

## データ構成

WebユーザーデータはIndexedDB `osuHubDB` へ保存します。

- `results`
- `coaching`
- `practice`
- `settings`

公開GitHubへ個人プレイデータを自動保存する処理は入れていません。

## GitHub Pages対応

追加:

- `.github/workflows/pages.yml`

Web公開Artifactへ含める対象を次のみに限定しています。

- `index.html`
- `pages/`
- `css/`
- `js/`
- `data/site.json`

Electronソース、bat、Desktop補助スクリプトはPages公開Artifactへ入れません。

GitHub Repository Settings側でPages Sourceを `GitHub Actions` に設定する必要があります。

## リポジトリ名変更への対応

旧:

```text
EliteMay/osu-setup-launcher
```

新:

```text
EliteMay/osu-hub
```

以下を新URLへ変更しました。

- `data/config.json` のDesktop Launcher更新確認URL
- `version.json` のRelease URL
- `data/update-version.example.json`
- Web側Desktop ToolsのReleaseリンク

## Desktop Launcher

既存Launcherを壊さないため、現時点では本体ソースの物理移動を行っていません。

追加:

```text
desktop/setup-launcher/README.md
```

現行本体は従来どおり以下を利用します。

- `src/`
- `tools/`
- `data/config.json`
- `package.json`
- `start.bat`
- `install.bat`
- `install-svcl.bat`
- `install-nircmd.bat`
- `build-installer.bat`

## 変更した主なファイル

### 新規

- `index.html`
- `css/styles.css`
- `js/storage.js`
- `js/app.js`
- `pages/coaching.html`
- `pages/results.html`
- `pages/practice.html`
- `pages/stats.html`
- `pages/settings.html`
- `pages/tools.html`
- `data/site.json`
- `desktop/setup-launcher/README.md`
- `.github/workflows/pages.yml`
- `仕様書.md`

### 更新

- `README.md`
- `作業報告書.md`
- `data/config.json`
- `data/update-version.example.json`
- `version.json`

## 崩してはいけない仕様

### Web

- APIキーを公開コードへ入れない
- プレイデータをGitHubへ自動送信しない
- GitHub Pagesで相対パスが壊れない
- IndexedDB保存を維持する
- JSONバックアップ / 復元を維持する
- AI CoachingはAPI課金を必須にしない

### Desktop

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer の一括起動
- ユーザー設定を更新時に維持する
- osu!本体の自動操作・プレイ補助は行わない
- 外部ツール本体や個人PC固有パスを公開リポジトリへ直接含めない
- 失敗時はログに理由を残す
- `start.bat`はエラー時に一瞬で閉じない

## 確認済み

- `EliteMay/osu-hub` へ書き込みできること
- 既存Electronソースが削除されていないこと
- Web用HTML/CSS/JSがリポジトリへ追加されていること
- Desktop Launcherの更新URLが `osu-hub` を向いていること
- Webデータ保存先がIndexedDBになっていること
- GitHub PagesワークフローがWebファイルだけを公開対象にしていること
- README / 仕様書を新構成へ更新したこと

## 未確認

- GitHub Pages Repository Settingsで `GitHub Actions` が選択されているか
- GitHub Pagesの実URLで全ページが正常表示されるか
- 実ブラウザでIndexedDBの保存 / 復元を一通り操作した結果
- JSZip CDN経由のZIP生成を実ブラウザで実行した結果
- スマホ実機表示
- Windows実機でDesktop Launcherがリポジトリ名変更後も正常起動するか
- GitHub ActionsでSetup.exe生成が成功するか
- GitHub Releasesの初回配布

未確認項目は確認済みとして扱っていません。

## 今後必要な作業

1. GitHub Pagesを有効化して公開URLを確認
2. Home → 各ページのリンク確認
3. Results / Practice / SettingsのIndexedDB動作確認
4. AI Coachingで実際のリザルト画像を多数入れて提出ZIPを作成
5. そのZIPをChatGPTへ渡して分析品質を確認
6. AI返却JSONをWebへ戻す実運用を試す
7. 実際の利用結果をもとにAI Coachのprompt/schemaを改善
8. osu! API、譜面管理、Replay、Skin、Collections等を段階的に追加
