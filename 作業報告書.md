# 作業報告書 - Setup Launcher v0.18.1 音声Fallback修正

## 目的

Setup Launcher v0.18.0を実Windowsで実行した際、`FxSound Speakers` はSVCL側で対象一致し `/SetDefault` も実行された一方、その後のWindows Fallbackが `0x80004002 (E_NOINTERFACE)` で失敗した問題を修正する。

`web-project-guide` の最新 `README.md` / `START_HERE.md` を確認し、既存Electronバグ修正ルートに従う。Windows固有機能はCI成功だけで実機成功扱いにしない。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 実機Evidence

v0.18.0のLauncherログで以下を確認した。

- `FxSound Speakers` のSVCL照合: 成功
- SVCL `/SetDefault <target> all`: 実行
- `DefaultRenderDevice*` / CSVによる確認: 確認できず
- Windows Fallback開始
- `AudioSwitcher.GetRenderDevices()` で `E_NOINTERFACE (0x80004002)`
- エラー表示に `IMMDeviceCollection` のIID `0BD7A1BE-7A1A-44DB-8397-C0A53CAD458F` が出力
- OpenTabletDriver起動: 成功

## Root Cause

`tools/AudioSwitcher.cs` の `IMMDeviceCollection` IIDがWindows SDKの定義と一致していなかった。

誤:

```text
0BD7A1BE-7A1A-44DB-8397-C0A53CAD458F
```

正:

```text
0BD7A1BE-7A1A-44DB-8397-CC5392387B5E
```

このため `EnumAudioEndpoints` が返すCOMオブジェクトを `IMMDeviceCollection` としてQueryInterfaceできず、Fallbackのデバイス列挙開始時点で失敗していた。

Microsoft / Windows SDKのMMDevice API定義と照合して特定した。

## 変更内容

### `tools/AudioSwitcher.cs`

- `IMMDeviceCollection` IIDをWindows SDKと一致する値へ修正

### `tools/switch_audio_device.ps1`

Fallbackを次の順序へ変更。

1. bundled `svcl.exe` がある場合、`/GetColumnValue DefaultRenderDeviceMulti` / `DefaultRenderDevice` / `DefaultRenderDeviceComm` を直接実行
2. `Name` / `Command-Line Friendly ID` が対象名と一致すれば、その時点で既定出力確認成功
3. SVCLで確認できない場合のみCore Audioでデバイス列挙
4. PolicyConfigで3 roleを切替
5. Multimedia default device IDをread-backして対象IDと一致確認

SVCL `/GetColumnValue` は公式ドキュメントの直接コマンド形式を使い、Fallback script側では `/Stdout` を前置しない。

### Regression Guard

`tests/validate-audio-interop.mjs` を追加。

- 正しい `IMMDeviceCollection` IIDを必須化
- v0.18.0の誤ったIIDが残っていれば失敗
- SVCL direct verification markerを確認
- `/Stdout /GetColumnValue` がFallbackへ再混入した場合は失敗

`.github/workflows/build-windows.yml` でinstaller build前にこのValidatorを実行する。

### Version / Docs

```text
Desktop: 0.18.1
Web: 0.2.10
configVersion: 17（変更なし）
```

更新:

- `package.json`
- `version.json`
- `data/site.json`
- `pages/tools.html`
- `README.md`
- `CHANGELOG.md`
- `tools/README_audio_switch.md`
- `PROJECT_LEARNINGS.md`

## Compatibility

変更しないもの:

- Electron userData保存方式
- `configVersion: 17`
- 既存の音声デバイス設定
- OpenTabletDriver path
- REAL path
- osu!lazer path / 自動検出
- Web IndexedDB / Backup Schema
- Supabase Account Sync

Migrationは不要。

## Verification State

### Static / Source確認済み

- `web-project-guide` 最新README / START_HERE確認
- v0.18.0実機ログから失敗地点をFallbackのCOM QueryInterfaceへ限定
- Windows SDK / Microsoft Core Audio定義で `IMMDeviceCollection` IIDを照合
- NirSoft公式仕様で `DefaultRenderDevice*` と `/GetColumnValue` を確認
- `PROJECT_LEARNINGS.md` にRoot Cause / Regression Guardを記録

### PRで確認する

- audio interop regression validator
- Electron JavaScript syntax
- Windows installer build
- Setup.exe存在・最低サイズ
- Web validation
- PRではReleaseを作成しないこと

### main merge後に確認する

- GitHub Release `v0.18.1`
- `osu_setup_0.18.1_setup.exe` upload
- final mainのCI / Pages状態

### 実Windowsで再確認が必要

- FxSound Speakersへ実際に切り替わること
- Launcher上で音声切替がsuccess表示になること
- `E_NOINTERFACE` が再発しないこと
- OpenTabletDriver / REAL / osu!lazerの一括起動Regression
- update install後に既存userData設定が維持されること

未確認項目は確認済みとして扱わない。
