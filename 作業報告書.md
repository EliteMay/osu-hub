# 作業報告書 - Setup Launcher v0.18.3 FxSound Core Audio照合修正

## 目的

v0.18.2実Windowsログで、SVCL側では `FxSound Speakers` を正しく検出して `/SetDefault` まで実行できた一方、Windows Core Audio Fallbackで `Device not found: FxSound Speakers` となる問題を修正する。

作業開始時に `EliteMay/web-project-guide` の最新README / START_HERE / Electron / Development Observabilityを確認した。最新Guideは1.7.0。既存Projectの保存形式・主要UI・One-click Update方式は変更しない。

## 実機Evidence

v0.18.2の実行ログ:

```text
matched: FxSound Speakers | FxSound Audio Enhancer\Device\FxSound Speakers\Render
DefaultRenderDevice*: 2- Arctis GameBuds ...
Windows標準Fallback失敗
Device not found: FxSound Speakers
```

v0.18.1で修正した `E_NOINTERFACE` は再発していない。今回の失敗地点はCOM初期化ではなく、Core Audio側デバイス一覧とSVCL側表示名の照合。

## Root Cause

SVCLとWindows Core Audioでは同じEndpointでもFriendly Nameの表記が異なる場合がある。

代表例:

```text
SVCL:        FxSound Speakers
Core Audio:  Speakers (FxSound Audio Enhancer)
```

従来FallbackはPowerShellで連続部分文字列として `*FxSound Speakers*` を探していたため、同じ単語を含んでいても語順が違うCore Audio Friendly Nameを一致扱いできなかった。

## 修正

`tools/switch_audio_device.ps1` に以下を追加。

- 音声名の正規化
- `audio / device / render` 等の一般語を除いたToken抽出
- 完全一致 / 部分一致を優先したScore
- Tokenが語順に依存せず全て含まれる場合のFallback match
- `Find-BestRenderDeviceMatch`
- `MATCHED_CORE_AUDIO` 診断ログ
- 未一致時の `MATCH_TOKENS` / `AVAILABLE_DEVICES`

これにより `FxSound Speakers` は `Speakers (FxSound Audio Enhancer)` と同一候補として扱える。

## Regression Guard

PowerShell Scriptへ `-SelfTest` を追加し、次のFixtureをWindows CI上で実行する。

```text
Expected: FxSound Speakers
Candidate: Speakers (FxSound Audio Enhancer)
Expected result: match
```

成功時:

```text
AUDIO_MATCH_SELF_TEST_OK
```

`tests/validate-audio-interop.mjs` でもToken matcher / Self Test / CI実行を必須化する。

## Version / Distribution

```text
Desktop: 0.18.3
Web: 0.2.12
Config Schema: 17 (変更なし)
```

v0.18.2にはOne-click Updaterが入っているため、v0.18.3をmainへ公開後はv0.18.2からアプリ内更新で取得できる。

## 崩していない仕様

- Electron `userData`保存方式
- `configVersion: 17`
- SVCLを第一経路として使う方式
- Windows Core AudioはFallbackとして使う方式
- OpenTabletDriver / REAL / osu!lazer起動
- GitHub Releases + `latest.yml` + `.blockmap` のOne-click Update
- Web IndexedDB / Supabase同期

## 検証予定

PR最終Commitで:

- JavaScript syntax
- Audio interop static validator
- PowerShell 5.1 parse
- `AudioSwitcher.cs` compile
- FxSound name-order matcher Self Test
- Auto Update regression validator
- NSIS installer build
- `latest.yml` / `.blockmap`整合
- Web validation

main merge後:

- v0.18.3 Release生成
- Setup.exe / latest.yml / blockmap upload
- GitHub Pages deploy

## 実Windowsで未確認

CIは実際のFxSound Endpointを持たないため、次はv0.18.2からv0.18.3へOne-click Updateし、実機で一括実行して確認する。

期待ログ:

```text
MATCHED_CORE_AUDIO: Speakers (FxSound Audio Enhancer)
SWITCHED_TO: Speakers (FxSound Audio Enhancer)
VERIFIED_DEFAULT: Speakers (FxSound Audio Enhancer)
```

実機成功までは音声切替を完成扱いにしない。
