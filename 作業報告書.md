# 作業報告書 v17

## 今回変更した内容

- `EliteMay/osu-setup-launcher` へv17ソース一式を移行
- `src/`、`data/`、`tools/`、bat類をGit管理対象へ追加
- `.gitignore`で`node_modules/`、`dist/`、`logs/`、外部音声ツール本体、生成物を除外
- 公開用`data/config.json`から個人PC固有のパスを削除
- 実際のユーザー設定はElectronの`userData/config.json`へ保存する仕様を維持
- 更新確認URLをGitHub Rawの`version.json`へ固定
- `version.json`の配布先をGitHub Releasesへ設定
- `data/update-version.example.json`を現行仕様へ更新
- GitHub ActionsのWindowsビルドワークフローを追加
- Setup.exeへSVCL / NirCmdなどの外部exeを直接同梱しないようbuild対象を調整
- SVCL音声切替で、切替前の対象照合と切替後の既定デバイス再確認を追加
- README.mdをGitHub運用向けに更新

## 変更した主なファイル

- `src/main.js`
- `src/preload.js`
- `src/renderer/index.html`
- `src/renderer/styles.css`
- `src/renderer/app.js`
- `data/config.json`
- `data/update-version.example.json`
- `tools/*`
- `start.bat`
- `install.bat`
- `install-svcl.bat`
- `install-nircmd.bat`
- `build-installer.bat`
- `clear-builder-cache.bat`
- `debug_info.bat`
- `.github/workflows/build-windows.yml`
- `.gitignore`
- `package.json`
- `version.json`
- `README.md`

## 崩してはいけない仕様

- 音声切替 → OpenTabletDriver → 遅延対策アプリ → osu!lazer の一括起動
- ユーザー設定を更新時に維持する
- osu本体の自動操作・プレイ補助は行わない
- 外部ツール本体や個人PC固有パスを公開リポジトリへ直接含めない
- 失敗時はログに理由を残す
- `start.bat`はエラー時に一瞬で閉じない

## 確認済み

- GitHubリポジトリへの書き込み権限
- `main`ブランチへ主要ソースが反映されていること
- JavaScript構文チェック
- `package.json` / `data/config.json` / `version.json`のJSON形式
- 更新確認URLが`EliteMay/osu-setup-launcher`のRaw `version.json`を向いていること
- 公開用初期設定に個人PC固有パスを含めていないこと
- 外部ツールexeをGit管理・Setup.exeの対象から除外していること
- GitHub ActionsのWindowsビルド定義が追加されていること

## 未確認

- Windows実機でのGitHub版`start.bat`起動
- Windows実機でのSVCL音声切替
- OpenTabletDriver / 遅延対策アプリ / osu!lazerの実起動
- GitHub ActionsでのSetup.exe生成成功
- GitHub Releasesの初回配布

## 今後必要な作業

1. Windows側でリポジトリをcloneまたはZIP取得して`start.bat`を確認
2. OpenTabletDriverと遅延対策アプリのパスを初回設定
3. SVCLを自動取得して目的の音声デバイスを設定・確認
4. GitHub ActionsまたはローカルでSetup.exeをビルド
5. 初回Releaseを作成し`version.json`の配布URL動作を確認
