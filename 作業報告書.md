# 作業報告書 - Cloudflare Worker本番deploy準備

## 今回の目的

`EliteMay/web-project-guide` Guide Version `1.2.0` を基準に、既存のCloudflare Workerを本番deployできる正式経路をGitHubへ追加する。

既存のWeb Runtime、IndexedDB、Account Sync、AI Coaching、Electron Launcher本体は変更しない。

Project Profile:

```text
STATIC + DATA + AI-HANDOFF + CLOUD + ELECTRON + TOOL
```

## 変更した内容

### GitHub Actions deploy経路

新規追加:

```text
.github/workflows/deploy-worker.yml
```

Workflowは `workflow_dispatch` の手動実行のみとした。

理由:

- 初回Secret未設定の状態でmain pushごとにdeploy失敗を出さない
- 本番deployを意図した操作として分離する
- 一時的なWorkflowを使ったファイル書換えには利用しない

### 必須Repository Secrets

```text
CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_TOKEN
OSU_CLIENT_ID
OSU_CLIENT_SECRET
```

秘密値はRepositoryの通常ファイル、GitHub Pages、`data/site.json`、公開JavaScriptへ入れない。

`CLOUDFLARE_API_TOKEN` はCloudflare Workers編集に必要な範囲へScopeを限定する前提。

### deploy処理

Workflowでは以下を実施する。

1. 必須Secret 4件の存在確認
2. `actions/checkout@v6`
3. `cloudflare/wrangler-action@v4` を利用
4. Wrangler `4.127.1` を指定
5. `cloudflare/worker` から `wrangler deploy`
6. `OSU_CLIENT_ID` / `OSU_CLIENT_SECRET` をWorker Secretとして反映
7. deploy URLを取得
8. `${deploymentUrl}/health` を確認
9. `ok=true` / `configured=true` / `service=osu-hub-api` を検証
10. GitHub Actions SummaryへWorker URLを表示

### ドキュメント

更新:

- `cloudflare/worker/README.md`
- `CHANGELOG.md`
- `作業報告書.md`

Worker READMEへGitHub Actions方式とローカルfallbackの両方を記録した。

## 変更していないもの

- `cloudflare/worker/src/index.js`
- `data/site.json` の `osuApi.workerUrl`
- IndexedDB Schema / Store
- Web画面Runtime
- Electron Launcher
- Desktop Launcher設定

本番Worker URLがまだ確定していないため、`data/site.json`へ推測URLは書いていない。

## Security

- Cloudflare API Tokenを公開ファイルへ保存しない
- osu! Client Secretを公開ファイルへ保存しない
- Temporary Cloudflare deploymentのclaim URLを公開GitHub Actions logへ出す方法は採用しない
- Workflowへ渡したosu! Secretは `wrangler-action` のWorker Secret機能を通してCloudflareへ登録する
- `/health` はSecret値を返さず、設定済みかどうかだけ確認する

## Dependency / External Action

Cloudflare公式 `cloudflare/wrangler-action@v4` を採用した。

WranglerはWorkflow内で `4.127.1` を明示する。

Root Electron / Cloudflare Workerの `package-lock.json` は今回も生成していない。今回、Workerのnpm dependency自体を変更していないため、推測したlock fileは作成しない。

## Verification State

### Implemented

- GitHub Actions本番deploy Workflow
- 必須Secret確認
- Worker Secret反映設定
- deploy後 `/health` 自動検証
- deploy URLのActions Summary表示
- deploy手順ドキュメント

### Static validated

PR作成後、既存 `Check web` が対象変更を含む状態で成功することを確認する。

### Not verified

- GitHub Repository Secrets 4件の実設定
- Cloudflare Worker本番deploy
- 本番Worker `/health`
- `data/site.json` への本番Worker URL反映
- 実osu!アカウントでRecent Scores同期
- Account Sync → Results → Statsの実ブラウザE2E
- Windows実機Desktop Launcher

## 現在必要なユーザー操作

Secretはチャットへ貼らず、GitHub Repository SettingsのActions Secretsへ直接登録する必要がある。

登録後に `Deploy osu Hub API Worker` Workflowを実行すれば、本番Worker URLまで取得できる。

そのURLが確定した後、`data/site.json`へ反映し、GitHub Pagesの既定Account Sync先として固定する。

## 次に必要な作業

1. Pull Request CI確認
2. mainへmerge
3. Repository Secrets 4件を設定
4. `Deploy osu Hub API Worker` を手動実行
5. deploy URLと `/health` 成功を確認
6. `data/site.json`へ本番Worker URLを反映
7. GitHub Pages再deploy確認
8. 実osu!アカウントRecent Scores同期
9. Results / Stats反映の実ブラウザ確認
