# Account Sync uses osu!'s public web profile endpoints.
# No OSU_CLIENT_ID / OSU_CLIENT_SECRET values are required.
# Add local-only Worker variables here only when a future feature explicitly needs them.
